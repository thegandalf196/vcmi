# V5 new Sorcery-subject proof — SOURCE ONLY until authorized render

Actual user remains unsure about V4 and asks for something more like Sorcery.
Book/key is NOT selected iconography; do not keep polishing it as approved motif.
Read updated NH_SORCERY_STYLE_BRIEF. Proposed response is a magician's expressive
hand actively shaping an open silver-violet thread. This subject itself remains
unapproved. One master/native proof only, not full family or final style guide.

## Actual production method

New original Blender scene.py, no imported hand mesh, texture, image, font or
proprietary reference pixels. Palm/thenar/wrist volumes and articulated five-digit
joint/phalange geometry are explicitly posed, joined with voxel remeshing and
softened geometrically. This is algorithmic modeling, NOT manual sculpting or
painting. Sparse palm creases and subtle curled-finger nails support anatomy.
Index and thumb control an OPEN spatial curve; three other digits curl. Small
separate arc echo suggests spatial displacement, not lightning/flame/orb/neon ring.

A substantial mulberry velvet sleeve with curved embroidered cuff anchors the
hand. No book/key or competing prop. Broad skin pigment fields, restrained matte
material response, selective silver highlight and theatrical warm light with weak
violet bounce retain the illustration-oriented hypothesis. No shader noise,
posterization, pixelation or generic painterly filter. Emission is deliberately
weak and limited to thread; it is not a VFX light ring. Anatomical/gesture/readability
quality must be checked from actual output, not inferred from modeled finger count.
No claim of painterly finish simply because a renderer is used.

All original code/geometry/materials/outputs CC0-1.0 per ../LICENSE. Tools retain
own licenses; no reference rights transferred. Prior V3/V4 and rejected art are
read-only comparison evidence and preserved. No paid service/account/upload.

## Quiet scheduling and next bounded proof

Prepared source during active947 GUI quiet lease, no renderer run. After Build's
verified teardown, send exact source hash/command and request ONE180sTERM/+10kill
CPU2threads32sample768slot. No execution until explicit GO:

```
timeout --signal=TERM --kill-after=10s 180s blender --background --factory-startup --threads 2 --python assets/new-horizons/sorcery-art/conjuring-hand-proof-v5/scene.py -- --samples 32
# Only if successful:
python3 assets/new-horizons/sorcery-art/conjuring-hand-proof-v5/reduce.py
```

Voxel-union execution time must fit the same bound; not a separate unbounded job.
On actual0, clean raw metadata into pixel-identical master-review.png, actual44/32
and optional aspect-preserving82x93 views; provide V4/V5 native comparison. Raw
master/.blend/settings remain PRIVATE/ignored pending independent privacy review.
No rerender repeatability claimed before tested. Inspect actual anatomy, full
silhouette, thread/no-neon character, light/materials, native meaning and metadata.
Preserve concrete failures, request repairs through Build rather than silently
rerendering. Report actual command/exits/elapsed and BlenderPID teardown.

User reviews master and native views, then chooses/rejects. No fullfamily, accepted
style guide or live import before explicit approval. Final20 assets, reproducible
editable source/provenance, Frontend/Build import and soleContent composed GUI plus
explicit final visual approval remain required. No game launch or commits by Artist.
