# New Horizons Windows x64 archive acceptance

## User-priority TWO-school previews — both independent package gates PASS

Final paired delivery is Windows1+Linux2; full evidence is
`testing/two-school-final-independent1/combined-summary.json`.
Windows: `build/two-school-preview-windows1/New-Horizons-Two-School-Preview-Windows-x64.zip`,
SHA `bdfe4e4e1acacfae6c8fe249dbb6aaecc6dce2d455fcc926157c79c014cba4fb`;
25,458,090 bytes, all1225 archive/stage files and1224 checksums checked,26 unchanged
PEs, full exact allowlist/helper/profile/README/two border-pointer deltas and source
companions. Linux2 archive SHA
`b990ddf9b3b538209e5d64693b4949fdba3a139a6d970a2654a7b91acf14cdbc`
passes all1175 files/1174 sums/native ELF bodies/modes and three source companions.
Both contain identical54 art/descriptor files; Light24 decoded/transparency/template
checks and native-size offline visual sheets independently reviewed. Ten emblem/
button images remain unbound. Profiles are distinct from normal and Sorcery-only.

Linux1 mode-metadata RED preserved; Linux2 changes only identity/checksums and
corrects current helper modes. Old Sorcery3f1c ZIP unchanged. Actual Linux gameplay
still requires a fresh guarded handoff; no Windows player/helper execution, new
screens, public rights or final visual approval. Linux413/packaging83cf and Windowsd90
are different engine baselines, with no cross-save claim. Both private bundles were
terminally cleared for user delivery without waiting on unrelated implementation.

Separate spell-UI FULL34691172003 succeeded for commit168217055. Build's sole
three-artifact download terminated in134.853s without retries. Its independent
static package audit now passes below; it does not replace the frozen art previews.

## Separate168217 Windows FULL — independent static package PASS

`testing/windows-168217-audit/final-summary.json` binds actual run34691172003 and
all three raw API digest/CRC/extracted-member bodies, then independently checks
1211 inner files/1210 sums,26 AMD64 images/6155 imports,945 resources (493 exact,
452 CRLF-only),4717 Git/submodule blobs (624 exact,4093 CRLF-only) and120 executable
mode differences. Producer extraction independently matches every byte. Three
current selected CRT bodies/versions bind;24 vendor DLLs match the actual public413
ZIP. Both actual dependency archives were streamed:113700 records/104497 files/
14 links unchanged, and all227 notices INCLUDING Microsoft PROVENANCE unchanged.

Player SHA `08584a89be47107bf1bb4a465f4349109605bde4b433b46114f55f1c8930baa3`;
source SHA `7f6ed91d9da915cbc01608e8389e5eafbc99510d8e4b4584d5aa3ae207f2ed61`;
dependency SHA `597a05fbd6a08c7c0c8ebe5d3f2c47bda2f0cb2c750fa7207c47b052580a6a5c`.
All three companion checksums independently agree.201 approved CI profile-path
hits in9 PEs, zero unknown PE profile paths; source ASCII inventory remains70 hits
in19 files including legacy osx/dmg_DS_Store /users/admin. NOT source-path-free.
Official redist terms and prior conditional source/notice qualifications retained.

85 CI package regressions reported PASS, clean tracked CI tree; ENABLE_TEST OFF.
No Windows helper/client execution, native C++ tests or native Windows gameplay.
New-engine normal/cancel/defender/stale/lifetime UI gates remain open. No art-preview
replacement/publication. Initial inspector camelCase-vs-raw-REST key error is
preserved in intake.log, repaired before any extraction; not a product defect.

## Earlier user-priority private Sorcery preview — independent package PASS

The user-authorized private art-only preview uses the audited d90 player, not a
new engine build or replacement of the later aaed audit. Local ZIP:
`build/sorcery-art-preview-windows1/New-Horizons-Sorcery-Preview-Windows-x64.zip`
SHA256 `3f1c9a8d6f14575c8843007d4058bd5b90099283c0346b732e56b7b567ea2777`,
25,316,780 bytes. Independent
`testing/sorcery-preview-intake-independent1/final-summary.json` verifies1220
stage/ZIP files,1219 checksums,26 unchanged PEs, exact content/helper/docs allowlist,
three descriptor frame orders and matching source/dependency archives alongside it.
All24 selected PNGs decoded RGBA with matching hashes/dimensions/CRC, header top28
clear and four border centers clear; seven foreground histograms and28 provenance
file bindings separately checked. No selected PNG text/EXIF metadata chunks.

Private profile is `%LOCALAPPDATA%\HeroesIII-NewHorizons-SorceryPreview\`.
Helper is exactly four profile-token substitutions from the base; five dirs match
and inherited pre-write mismatch refusal remains. Initial inspector assumption
that the refusal block was new is retained as a RED, not a product defect.
No PowerShell, PE or native Windows gameplay was executed. README and Tester
`USER-RETEST.md` explain separate hero/rank/spell contexts; no all-state save is
included. Five emblem/button PNGs are provisioned, not claimed currently visible.
Preview integration permission is not final artistic approval or public rights:
composites include template-derived pixels; no public upload. Build was sent the
terminal qualified PASS for immediate private-user delivery, without waiting for
Focus Fire, Light or full redesign acceptance.

## Latest aaed fixed-artifact checkpoint — scoped independent package PASS

`testing/windows-aaed-audit/summary.json` binds five independently produced
reports for FULL34272556634 at aaed0677960d980f0180ac69926077ca61997215.
All1211 current package bodies rehashed;1210 sums,26 AMD64 PEs/6155 imports,
945 resources (493 exact/452 CRLF-only),4715 Git/submodule source blobs
(624 exact/4091 CRLF-only) and120 executable-mode differences accounted.
Privacy scan independently reproduces201 approved CI service-path hits in9
images, no unknown profile paths. CI package/source regressions are85PASS,
zero failures/errors/skips and a clean tracked CI tree; ENABLE_TEST=OFF means
this is not native C++ regression or Windows gameplay acceptance.

The actual comparator is **413930, not d90**. Dependency archives pre/post pinned:
baseline25a2ec71aefe59fd396f5795b41e0cbc187a001184ca31547eb99e1d4b0cdae1,
currentab65ae361f42216e473df68e6921947eae9c8ade66f97ae5f3ebd60426233ed1.
Package comparator3c360ea58ba02f938fd16e26705895bfb36cf0c4634b4b29ca9c8e0c78eaac8f;
current148741dfb080ac5b95d6040358b982b2b5fff2179b792a953fd41669da045133.
All113700 records/104497 files/14 links match in bodies/types/modes/targets;
**227 notices including Microsoft PROVENANCE are byte-identical**. Producer's226
excluded PROVENANCE, not a claim that it differed. Earlier inspector assumption
that one notice must change is preserved in dependency-notice.log; corrected
pinned full comparison passed, not a weakened arbitrary-delta acceptance.

Current selected CRT artifact independently binds three packaged14.29.30157.0
PE versions and source hashes under14.29.30133, distinct from14.51 notice
provenance. VS2026-Redist.txt is a pointer, not a filename list. The bounded
Microsoft official-page fetch and exact recursive VC/redist permission/debug
exclusions are preserved with two pinned DOCX terms. Licensed-user/distribution
conditions remain; no legal warranty or retrospective origin proof. Earlier
pointer/escaping/parser assumptions remain in logs. All24 vendor PE bodies equal
413. Source/notice historical rights qualifications remain in force.

This is **SCOPED_PACKAGE_AUDIT_PASS_WITH_QUALIFICATIONS**, not publication,
PE execution, native Windows play/audio/save acceptance or full redesign closure.
No duplicate download, d90 replacement or artifact mutation. Linux host-library
drift is separate and old native fences are not current launch authority.

## New source47 foundation — package regression review only

Actual commit d90c2ea7ae80456eae06f17f7286cf8debd8e705 independently matches
reviewed47 blobs/tree459472/parentdb2f and prescribed identities. Producer Windows
preflight JSON/log inspected:85PASS/0SKIP at db2f plus dirty source; no fresh Tester
rerun or clean d90/native Windows execution claim. Evidence:
`testing/commands-static/final47-commit-windows85-independent.json`.
Fresh independent API now verifies FULL34193282905 **success** at d90, actual
compile/package/upload with preflight-only/repack substitutes skipped. Own fresh
artifact API queries and rehashed raw ZIPs bind player10044916251 (5f382a36)
and CRT10043446038 (1e239579), with CRC and all five readonly extracted bodies
verified; no duplicate download. Actual playerZIP0fbd has1211files/1210sums,
26AMD64 PEs/6153resolved imports and201approved CI-only profile paths across9
images, no unknown paths. All945 common resources match pinnedGitd90
(493exact/452text-CRLF-only), with Windows dirs/helper bodies separately matched.
4705 source blobs match Git (624exact/4081CRLF-only), four submodule pins and20
exclusions;120 Unix executable-mode differences remain recorded, not erased.

Independent exhaustive comparison verifies113700 dependency members
(104497files/14links) and226 notices identical to audited413, except current CRT
provenance separately bound. All24 vendorDLLs, dependency/media metadata and
launcher/helper bodies are byte-identical to413. Current selected88c4 metadata
binds all three actual packaged14.29 CRTs, distinct from14.51 notice environment;
an independent raw PE RT_VERSION reader and three in-memory version-field
mutations were executed. Earlier `pefile` import failure is retained. New strict
text-suffix inspector REDs (SQL, then ERM) were also preserved; actual SQL/ERM/VERM
source review and distinct text-only repair passed without changing package bytes.
Evidence: `testing/windows-d90-audit/` (including `reuse-release-gate-map.json`).

**Publication HELD.** Default remains0.5.1/69. A separate normal default051/source47
client/readback gate now passed on Linux in `testing/default051-d90-readback2/`:
actual5374 load/eight tab inputs and screenshots/checked normal exit0 at97.570s,
all5418 controls unchanged and complete owned teardown. Old readback1 abort130
and expired approval REDs remain. This is a NO-book save and cannot cover changed
CSpellWindow/QuickSpellPanel paths. Separate default69 legacy-spell/battle UI
and private canonical-preference persistence evidence is still needed. Private070 native/acquisition work cannot substitute.
CI PowerShell setup success and unchanged tested scripts are not native gameplay.
No new publication/default70/full redesign/player approval; native Windows
execution, historical a0 origin and broader artwork rights limitations remain.

## Current413930 FULL — independent static and publication gates PASS

Actually inspected FULL34150419497 terminal success and both download exits0;
compile/package/upload succeeded, source-only and notice-only substitutes skipped.
Fresh own API queries bind artifact10030630291 and selected-CRT10029580331 to
that run/source413930. Five downloaded files independently hashed and verified
readonly/single-link. Outer GitHub ZIP digest is reported metadata, NOT verified
from gh's extracted files. No duplicate download or PE execution performed.

Player ZIP SHA256`3c360ea58ba02f938fd16e26705895bfb36cf0c4634b4b29ca9c8e0c78eaac8f`:
1206 files/1205 exhaustive sums, safe ZIP paths/CRC;26 AMD64 PEs/6142 resolved
imports.940 resources match Linux413 (493 exact,447 CRLF-only); helpers match
Git413.4672 source blobs (624 exact,4048 CRLF-only), four pins,19 exclusions;
120 executable-bit differences (116 lost,4 added) remain disclosed. Privacy:
201 approved hosted-CI references in9 PEs, zero unknown profile prefixes.

Same-run CRT selected14.29.30133 directory binds actual final three DLL hashes
and file version14.29.30157.0, separate from notice environment14.51.36231.
Microsoft terms and21 other vendor DLLs match independently audited891 bytes;
historical a0 origin remains unresolved. Exhaustive new/prior dependency TAR
comparison matched113700 member bytes/types/modes/links,104497 regular files,
14 links and226 notices (new current CRT provenance checked separately).
Windows source SHA`e22f4780…90d0`, dependency SHA`25a2ec71…dae1` are distinct
from Linux source/companion identities despite justified corresponding-source reuse.

Exact notes SHA`12fc273c…6234` and four sealed assets were approved; own fresh
API verified draft384295184, target413 and four complete uploaded digests/sizes
before publication. **Published20:09:16Z** at
`https://github.com/thegandalf196/vcmi/releases/tag/nh-preview-413930bc32d0-windows`.
Independent anonymous published API/notes/tag413 checks passed; all four public
asset sizes/digests match and anonymous HEAD requests finish200. One previously
missing tag autoarchive was downloaded:4491 entries/4105 root Git blobs, one
committed BAT CRLF conversion,19 exclusions/four unpopulated submodules. Full
already-owned ZIP/source assets were NOT downloaded again; no second public
ZIP-body byte-check claim. Linux's separate tag83cf/5 assets remain distinct.

Evidence: `build/new-horizons-linux/testing/windows-413930-audit/` including
package, source-mode, CRT, fresh API/download binding and dependency-delta reports.
These gates do NOT establish native Windows gameplay, audio, fresh save/reload,
or full redesign acceptance. Linux packaged413 readback103.097s
is separately bounded evidence, never a Windows proxy.

## Historical status and authority

Independent archive audit assigned to Tester. **Local original82 notice/source
repair independently passes the recorded archive gates; publication and native
Windows gameplay remain unverified.** Historical failed artifacts remain below;
the repaired package is not a Windows build of the later redesign. CI compilation, native tests, static inspection,
Linux acceptance and Wine do not establish Windows playability. Build owns the
workflow, downloadable artifact, setup/packaging tooling and release. Tester owns
this document and coordinated independent test helpers; current ownership follows
NH_WORKER_PLAN.md, not historical role assignments.
No GUI launch, software installation or modification of the Linux manual-preview
or saved profiles is authorized by this audit.

## Windows891 public prerelease independently verified

Anonymous API/tag requests independently verify
`https://github.com/thegandalf196/vcmi/releases/tag/nh-preview-89165787e50d-windows`
at actual891 commit, four uploaded asset sizes/digests and359-byte outer sums
SHA256 `1ac3bcc51e6c2879881b38f195de5031480f9ca7c77334bd9832f7dc64ddb702`.
Retained actual public player is byte-exact audited adcd0b6… ZIP, CRC1175 PASS;
no duplicate asset downloads. Actual public tag archive4409 entries matches4026
root Git blobs exactly plus committed BAT CRLF,16 exclusions/four unpopulated
submodules. Capability implementation is present; future mastery is absent.

Actual public notes match updated local draft. Independent reconstruction proves
**only** requested Limits bullet clarification changed from previously hash-bound
reviewed draft; no silent broader rewrite. Reports/scripts/logs:
`testing/windows-audit034/public891-independent.{json,log}` (exit0), independent
API/tag snapshots and `public891-notes-delta-independent.json`.
Bounded public-delivery PASS, **not native Windows gameplay or full redesign**.

## Windows891 full artifact — independent archive/source/CRT PASS

Actual retained terminal34110964044 at89165787e50df755ad6311365e039f3185325a09
completed success, watcher0. Client compile, package/upload and selected-CRT steps
succeeded; preflight-only/notice-repack branches skipped. No duplicate download.
Readonly `full-891657-artifact` ZIP24974039 SHA256
`adcd0b6ce62cb36400390ee07e1fc911c454f3a12930a19ef02e376443030a30` passes
CRC/path safety,1175 files/1174 exhaustive sums and three outer artifact identities.
26 AMD64 PEs resolve6123 imported symbols against bundled/declared Windows contracts.
Independent201 hosted-CI findings across9 PEs match exact offsets/provenance; no
unapproved personal prefixes.909 Linux891 resources match469 exactly/440 CRLF-only.

Fork source16967615 bytes SHA256
`e0d522934829af008e2fa9b4296b64ca1fcae81ac83b3fb4bf990bd24147347b`:
4594 Git blobs match600 exactly/3994 CRLF-only, four pinned submodules/16 exclusions.
**120 executable-bit mismatches =116 lost +4 added CMD/BAT bits**, not an exact
Unix-mode-preserving export. Preserve that caveat in release notes.

Independent anonymous API binds selected-CRT artifact10015034607 to this actual
891 run/SHA. Its eight selected records bind final three CRT DLLs by exact hash,
AMD64 version14.29.30157.0 and VS-relative selected14.29.30133 paths. Full terms
hash-match; notice environment14.51.36231 remains distinct. This is **same891-run
origin evidence**, not a351/a0 CRT-origin proxy. Other21 vendor DLL bytes match
previous independently cache-bound bytes. Historical a0 origin hold is unchanged.

Dependency source303766398 bytes SHA256
`f34425c7953d8bfbddc3874f453f06ed7798b02ba084e36d81dedf750a0a3a14`:
all113700 members byte/type/mode/link-identical to independently audited351 closure
(104497 regular files/14 links).226 license files are identical except separately
verified new CRT provenance. Actual README/SOURCE-NOTICE retain component/privacy/
setup/native-platform caveats; corresponding helper bytes match891 Git.

Evidence under `testing/windows-audit034/`: `msvc-891-package-check`,
`crt891-final-independent`, `deps891-delta-independent`,
`windows891-terminal-modes-independent` reports and scripts/logs (exit0).
Bounded prepublication notes reviewed at SHA256
`2d7b8f3e934fc77e1f65f85c2a213e025650750e70729485b1c3b7fd0277230f`;
no owned hard blocker. Minor wording clarification requested to separate current
unaccepted journeys from future features not included. Evidence:
`windows891-notes-independent.json`. Actual public assets/notes subsequently verified above. Native Windows
gameplay remains unverified; Linux, compilation, source and static PE inspection are not
Windows playability or full redesign acceptance.

## Windows351 public prerelease independently verified

Anonymous public API checks bind all four asset sizes/digests to audited local
player/source/dependency/sums bytes at
https://github.com/thegandalf196/vcmi/releases/tag/nh-preview-35119d5341b3-windows .
Actual remote tag resolves to35119d5341b3c237b87e71493ed8895cecdb2600.
Retained actual public player ZIP matches audited SHA256 and1173-entry CRC checks.
Actual public notes match the reviewed draft, including SDL3/201 hosted-CI paths,
GPL3 combination, CRLF/116 lost plus4 added executable bits, NONE and native Windows
limitations. Autoarchive4395 entries including root contains4012 exact root Git
blobs plus committed `android/gradlew.bat` CRLF conversion;16 exclusions and four
unpopulated submodules remain, so it does not replace the full source companion.
Evidence: `testing/windows-audit034/public351-independent.json`, script and exit0
log. This closes bounded publication inspection, not native Windows gameplay,
historical a0 CRT origin or full redesign acceptance. No duplicate asset download.

## Windows351 independent archive/CRT/source gate PASS — no native Windows gameplay

Sole readonly `full-35119d-artifact` independently passes ZIP CRC/path checks,
1173 files/1172 exhaustive hashes and all3 outer source/package SHA256 identities.
ZIP24955639 SHA`74e63ae25a2872d73b72c966b36fa7591c443daf82c3002272ea58d2d235d334`.
26 AMD64 PEs resolve6117 imported symbols against bundled exports/declared Windows
OS contracts.201 hosted-CI profile findings across9 PEs match exact offsets and
provenance;21 vendor DLLs equal previously independently public-cache-bound bytes.
907 c550 resources match469 exactly/438 CRLF-only; no future capability resources.

Actual fork source4580 blobs matches600 exactly/3980 CRLF-only, four pinned
submodules and16 committed exclusions. **120 executable-bit mismatches are116
lost bits plus4 added bits on CMD/BAT files**, not120 lost modes. The source archive
is not an exact Unix-mode-preserving export. Preserve this caveat in publication;
root license is not the whole combination: xBRZ GPL3 and dependency terms remain.

The SAME351-run selected CRT artifact10008343544 is independently API-bound to
34093695275. Its8 selected-source records bind the actual final3 CRT files by
SHA256, AMD64 PE version14.29.30157.0 and VS-relative source paths in14.29.30133.
Both full runtime/VS terms hash-match; notice-environment14.51.36231 is separate.
This closes **351** final binding, not historical a0 copied origin. All113700
source companion members match previously audited a0/a722 byte/type/mode/link
records, including14 links and nested source originals.226 license files match
previously reviewed bytes except the separately verified new CRT provenance.

Bounded package/source/privacy approval is not native Windows GUI, optional runtime
execution, full redesign or uninspected publication assets. Historical failures
remain. Evidence `msvc-351-package-check.json`, `crt351-final-independent.json`,
`deps351-delta-independent.json`, `msvc-{a0,351}-mode-correction.json` and scripts.

## Earlier Growth351 actual FULL terminal success — artifact audit was pending

Independent retained terminal JSON confirms34093695275 at
35119d5341b3c237b87e71493ed8895cecdb2600 completed SUCCESS, watcher exit0.
Actual selected-CRT precompile, client compilation, package and upload steps all
succeeded. This is not independent final-byte CRT/package/source/privacy approval
or native Windows gameplay. The sole Build-owned artifact download was held until
the Linux restart's verified teardown; that lease is now released. No duplicate
CI dispatch/download by Tester. Evidence `windows351-terminal-independent.json`.

## Earlier Growth351 portability successor — corrected FULL was pending

Independent retained terminal/log review confirms run34089398757 at322cbe failed
client compilation: `CPlayerInterface.cpp:505`, MSVC C2397 converting `const ui32`
to `int` in the queued aggregate. Actual source and CMake-selected CRT precompile
steps succeeded; those PASSes remain distinct from the later compilation failure.
No final package or final CRT binding was produced by that run.

Commit35119d5341b3c237b87e71493ed8895cecdb2600 contains exactly two paths: the
single `PrimaryGainSnapshot.level` change `int`→`ui32` in `GUIClasses.h`, and the
actual-declaration regression test. Independent non-compiling type case passes;
substituting the old `int` field in the test reader correctly fails. Snapshot header
SHA`dff10da82a1f3b701d17088252c9a1c6c02c4bf3e9d109317f7efbe2692464d1`.
Tester did **not** invoke the compiler-backed case or claim an independent85-suite
run; Build owns compilation. Source review does not prove rendered callbacks.

Build owns the single corrected FULL34093695275 terminal watcher/download lane.
At this earlier checkpoint its final compile, mandatory CRT binding,
package/source/privacy audit and native Windows gameplay were separate gates. Historical a0 origin/publication hold is
not repaired retroactively. Evidence `windows-audit034/snapshot-terminal-independent.json`
and rerunnable `snapshot_terminal_review.py` (retained logs, not duplicate CI).

## Full MSVC a0 artifact — archive/source checks, provenance question OPEN

Actual run34076721466/a0 completed SUCCESS, including compile/package/upload.
Independent audit uses the sole frozen `full-a0dcdd-artifact/` download, not a
second cloud transfer or the separately published MinGW preview. ZIP24918052,
SHA `ec67b38d836f1c07e5a2136105f83525f061728b1b7ea3fe92b7a46e971a0d87`:
1166 files/1165 exhaustive internal hashes, CRC/path safety and all three outer
archive hashes pass.26 AMD64 images/6106 import symbols inspected; all non-system
symbols resolve against bundled exports (Windows OS/API contracts are assumed,
including DbgEng as in the earlier MSVC audit, not native-OS execution proof).

All900 resources match Linux034:465 byte-exact/435 CRLF-only. Fork source has
4540 Git blob matches:596 exact/3944 CRLF-only; four pinned submodules/16 exclusions.
**119 executable-bit mismatches occur in the Windows filesystem export:115 lost
bits and4 added bits on CMD/BAT files** (corrected from earlier all-lost wording);
this is not an exact LF/Unix-permission source export.32 Conan records,113700
source member bytes/types/links and218 notices match the previously independently
audited actual a722 source artifact (188 raw source notices + SQLite dedication
+29 package notices). All21 vendor DLLs match pinned public-cache bytes.

Independent standard-pattern privacy scan reproduces191 CI references/nine PEs
and exact report offsets/hashes/provenance. An additional broad scan inventories
**10 MSYS `/c/users/runneradmin/.conan2/p/` references in five FFmpeg DLLs**, not
covered by the existing191 report. All five are exact public-cache images; this
is documented hosted provenance, not personal-workstation leakage. General MSYS
unknown-profile/traversal scanner coverage was requested, not silently suppressed.
The repaired guard is now independently checked:201=191 original+10 MSYS findings,
all new offsets/SHA identities exactly match the earlier independent cache audit;
remapped MinGW still has zero findings without any CI exception. Eighteen separate
ASCII/even-UTF16/odd-UTF16 contained/unknown/outside/space-traversal/prefix/drive
controls pass. Evidence `msys-guard-independent.json`, scannerdf4d71d8….

**Publication approval held for provenance clarification:** all three CRT DLLs
have actual FileVersion14.29.30157.0 and exactly match old82 bytes; Microsoft notice
PROVENANCE declares environment redist directory14.51.36231. The seven full terms
files match the previously inspected full-terms artifact, but directory-only
applicability does not identify the copied CRTs. Build must bind actual runtime
hash/version/origin to the appropriate terms or correct the provenance. This is
not an assertion of illegality or a demonstrated loader failure. No native
Windows gameplay claim and no MSVC publication claim.

Private evidence: `msvc-a0-package-check.json`, `msvc-a0-conan-check.json`,
`msvc-a0-cache-and-msys-paths.json`, `msvc-a0-crt-provenance-question.json`,
`msvc_a0_check.py`, and actual terminal API/log/exit records.

## Future CRT producer gate — independent source/fixture approval only

Reviewed `windows_crt_provenance.py`, its seven unit cases, final packager call and
workflow precompile gate. Independent complete offline runner: **82 tests,
0 failures/errors/skips**, source checkout c550 with explicitly dirty tooling.
The initial Tester invocation omitted required `--report` and exited2; that setup
mistake is retained separately from the corrected passing run.

Real PE decoder independently returns14.29.30157.0 for all three unchanged a0 CRTs.
A separate test uses those actual bytes and actual retained terms in a **synthetic**
VS directory: positive binding passes; eight controls reject ambiguous cache roots,
source escape/mismatch, missing/changed terms, term traversal, wrong PE machine and
unreviewed runtime family. No negative case rewrites provenance. Environment14.51
remains distinct from bound runtime14.29; emitted source paths are VS-relative.

The workflow inventories selected CRTs before compiling, records structured failure
metadata and always attempts evidence upload. The final package requires source-SHA
identity, actual AMD64/version and matching retained-term hashes. This approves the
bounded future prevention implementation, **not actual Visual Studio origin,
historical a0 provenance, a Windows CI result, gameplay or publication**. a0 stays
held and unchanged. Evidence `crt-{82-independent,binding-independent}.json`,
`crt_binding_review.py` and retained logs under `testing/windows-audit034/`.
Reviewed hashes: helper938a63fc… / tests19859ef1… / packagera9c6ff41… /
workflow5ca27590…. Future release artifacts still require independent auditing.

Actual FULL34087844032/bc376 then failed **before client compilation**: the real
precompile decoder could not import `pefile`. The retained terminal record, actual
failed-step log and once-downloaded structured artifact agree (`ModuleNotFoundError`).
The82 cloud fixtures were green but did not establish the real decoder environment.
This exposes a gap in Tester's earlier source review: real local decoding used an
already-equipped venv, while unit cases mocked decoding; the CI installer lacked
the dependency. Do not erase that failure or describe82 as full workflow acceptance.
Repair322cbe02548af0c9f02805453d98f0ec4587fae4 pins pefile2024.8.26 and performs a
real early import/version assertion before regression/CRT gates. Tester reread the
complete workflow and independently ran **83 tests,0 failures/errors/skips**
(`crt-83-independent.json`). Successor FULL34089398757 is separately monitored by
Build; dispatch does not prove the repair has passed on Windows or yield a package.

## Shared binary privacy guard / Linux distribution boundary

Independently reproduced frozen Linux034's two profile references: client
.dynstr/RUNPATH and libvcmi.so .rodata install prefix. Original941a.../ad7e... hashes
remain unchanged. Linux publication is blocked; gameplay evidence is unaffected.
ELF-magic scanning catches extensionless clients and versioned libraries, including
odd-aligned UTF16 control; clean `$ORIGIN` control passes. Initial10 privacy unit
cases passed. Found and reported existing missing-directory CLI false PASS;
subsequent repair now gives sanitized error JSON/exit1 and its updated unit suite
passes. Valid-directory scans and published35-PE MinGW bytes are unaffected.
No binary string patch accepted: future Linux delivery needs a clean-prefix,
relocatable build. Evidence `elf-privacy-independent.json`, preserved missing-root
control and separate fixed-control/unit logs.

## Latest remapped034 package — archive PASS / scoped preview readiness

**No archive/privacy/source blocker remains** in frozen `package-034-remapped/`:
ZIP37504301 bytes, SHA
`70a9daf3ae84650275c441372f87a4c4f8f87f9764167ed25d472643612d6802`.
Independent checks establish1171 files/1170 exhaustive internal hashes, CRC,
Windows path/collision/link safety, all five outer artifact checksums, and **zero
ASCII/UTF16 profile-path findings without a CI exception**. All35 actual ZIP PEs
match the independently parsed complete deployment:5872 import symbols resolve;
missing-DLL/export controls reject and restored data passes. Four GNU runtimes
and the exact Ogg alias are present. No native PE/game execution was performed.

All900 Linux034 resource files match; Windows dirs/helper bytes match packaging
Git902c4f33315347e06a68abc26a188393e7b420cf. Client hash86369507... and facade18cc0a84...
match frozen stamped identity; the facade contains the full034 revision. All4524
actual detached-build source files and mtimes match archived034/fb6c... bytes.
Read actual build/install/package exits0 and the four-step version-stamping log.
The earlier unstamped build was rejected by the existing identity gate and is
preserved, not silently published. Relative gitdir compatibility was handled by
Git metadata alias, not changed engine files or weakened validation. Initial
missing-Conan-PATH source collection failure is likewise separate from the later
successful isolated-environment collection.

All four actual source companions pass: exact engine archive;4540 packaging Git
blobs/four pinned submodules/16 reviewed exclusions; GNU ten-file companion and
full distribution license texts;26 exact remapped Conan host identities with
107718 source members byte/type/link-identical to the independently audited prior
source collection. All180 source notices and29 rebuilt package notices match.
Compared with rejected old ZIP,1133 files remain unchanged. Only five non-PE
metadata files changed, with verified identity/hash changes; added package notices
are Brotli and dav1d. GNU alias metadata changes only its new hash, and PE metadata
changes only verified rebuilt hashes. Original173-hit ZIP remains unchanged/RED.

**Publication assessment:** archive gates pass for an explicitly unsigned,
incremental commands/schools preview, with matching four source archives and sums
published together. This does not mark native Windows gameplay, outstanding
new-battle NONE/broader GUI journeys, or full redesign accepted. No publication
claim was made before upload. Publication is now independently checked at
[unsigned prerelease](https://github.com/thegandalf196/vcmi/releases/tag/nh-preview-0342384549c2-mingw):
anonymous GitHub API reports all six asset sizes/server SHA256 equal accepted
local files, downloaded player ZIP repeats70a9... and CRC pass, public anonymous
HEAD200. Actual GitHub tag autoarchive4352 members excludes NH diaries/encrypted
key while preserving licenses/design/build script. Tag export metadata is not the
compiled engine identity; use the explicit matching source companions. Initial
release body correctly disclosed native/full-redesign limits but omitted the
specific NONE/broader-GUI caveat claimed in the publication notice; requested a
notes-only correction from Build. A subsequent independent current-body reread
now confirms the explicit visible NONE/broader-GUI caveat, native Windows remains
unverified, and growth/masteries/tiers are explicitly absent from034. Evidence
`published-notes-correction-{raw,independent}.json`; original publication evidence
`published-release-independent.json` and `published-autoarchive-check.json`.

Evidence under `testing/windows-audit034/`: `remap-package-checks.json`,
`remap-pe-independent.json`, `remap-pe-negative-controls.json`,
`remap-conan-final-check.json`, `remap-final-differential.json`,
`remap-build-source-check.json`, and independent parser/package audit scripts.

## Earlier local MinGW034 package — static/source passes, privacy FAIL

The frozen `package-034-local-driver/` ZIP SHA-256 is
`28abdd59030763427e69a8278b8dcf0d73f93c2326434a140a353f130c6e0ce6`.
Independent inspection verifies1169 files/1168 exhaustive internal hashes, ZIP
CRC, safe noncolliding Windows paths, no links/encryption, all outer sums and four
source-companion identities. All35 PEs match the previously independently audited
images. All900 Linux034 resource files match exactly; the sole platform addition,
`config/dirs.json`, and three Windows helper files match packaging Git5af3a161e43c.
Engine notices match034 Git; packaging source has4535 exact Git blobs, four pinned
submodules and fifteen recorded exclusions. GNU source tar has the exact ten
previously audited companion files.

**Actual distribution/privacy FAIL:** nine PE `.rdata` sections embed173 occurrences
of the local personal home path: SDL3_mixer59, client3, vcmi library37, libopus64,
and five FFmpeg libraries two each. Not debug-only data; stripping debug sections
cannot fix this. No purchaser archives/saves or private paths in other ZIP entries
were found by this scoped check. Build independently reproduced the9/173 result,
withholds publication, and owns a source-path-remapped successor dependency build.
Frozen bytes are preserved, not string-patched. No current GUI GO.

Conan graph correction: `install.json`, not the earlier missing-package probe
`graph.json`, is the shipping graph. Its frozen copy under `feature-034-build-records/`
hashes to `c180d148bd74f47ec9f2de108db51477c74088ca20cd5a49f9f77644a89b1888`.
All26 host references/package IDs/revisions/settings/options match the actual ZIP
inventory; non-build unskipped edges close. The source archive has107718 members
under25 implementation roots; the remaining host dependency is Windows-system-only.
Safe contained links,98905 exact cached source files and63 exact recipe/export
files are verified. Two Boost Jamfile differences exactly reproduce its supplied
build() transformations; only omitted cache entry is generated machine-specific
`user-config.jam`, reconstructed by that same supplied recipe. No other entry gap.
All180 source notice payloads match their actual source archive and the remaining
27 package notices match the actual package cache. The reported209 bundle inventory
means207 notices plus DEPENDENCIES.json and source archive, not209 source files.

**Packaged GNU inventory/full texts pass this audit:** both copyright inventories
and all nine common-license files exactly match installed distribution texts;
every referenced `/usr/share/common-licenses/` target is included, including GCC
Runtime Library Exception and full GPL/LGPL terms. Earlier DSC/source/pin/runtime
checks remain applicable. This is evidence of supplied texts/sources, not general
legal advice, publication approval, native Windows setup or gameplay acceptance.

Evidence: `testing/windows-audit034/{package-checks,private-path-hits,
conan-source-check,closure-notice-check}.json` and `package_check.py`. Raw graph/cache
and diagnostic paths remain private. Full redesign, Windows execution and the
privacy-clean successor package still require their independent gates.

## Earlier034 feature components — preserved independent lineage

Build explicitly froze these local cross-build components for read-only audit.
This is separate from the original82/MSVC repair below and from the new cloud run.

- Actual `new-horizons-source-034238454.tar.gz` SHA-256
  `fb6c263361dd6b13aaaea4f9494e926690c74b9f39eef638f46858dee18c9c1e`:
  **4524 exact Git blob matches**, four complete pinned submodule trees and exactly
  fifteen reviewed credential/worker-diary omissions. No normalization needed;
  no duplicate/escaping member or symlink. Compared to034 commit and pinned bare
  Git objects, not mutable working files.
- Actual frozen `runtime-audit-034/`: independent byte-level PE parser checked
  **35 AMD64 images and5872 imported symbols**. All non-system DLLs/exports resolve;
  directory checks find zero delay-import descriptors and zero forwarded exports.
  Twenty explicit OS DLLs remain platform dependencies. `ogg.dll` and `libogg.dll`
  are byte-identical. Four GNU runtime hashes match the supplied provenance.
  This is loader-structure evidence, not Windows execution or runtime media proof.
- GNU companion: all three DSC source/version identities and all six descriptor-
  pinned archives/signature files match sizes/SHA-256, with no missing/extra file.
  Independent official Ubuntu/Launchpad HTTPS retrieval reproduces all three DSC
  bytes. Installed runtime package/source versions match the claimed identities.
  **105357955bytes including manifest**; PGP signatures preserved but not verified.
- Five outer GNU source archives pass member safety checks. GCC's nested upstream
  archive contains129835 members and newlib5658; no unsafe members/links found.
  GCC Runtime Exception/GPL/LGPL text locations/hashes recorded. These complete
  sources are not a claim that the future player ZIP contains all required notices.
  Python3.14's stream reader hit EOF handling on one compressed archive; ordinary
  compressed readers succeeded. No artifact corruption inferred from that tool issue.

Evidence: ignored `testing/windows-audit034/{fork-source-check,pe-independent,
  gnu-descriptor-check,gnu-independent-descriptors,gnu-archive-structure,
  gnu-nested-source-check}.json` and independent PE parser. Actual final wrapper,
Conan source bundle, installation/helper behavior and native Windows gameplay
were unaccepted at that checkpoint. The newer actual-ZIP audit above supersedes
the static package/source gaps, not execution or privacy acceptance.

Fresh MSVC workflow run34070234107 was independently queried in the explicit
private repository: exact034 commit, **completed failure before compilation**.
Failed step `step-security/msvc-dev-cmd@main` says a StepSecurity subscription is
required for private repositories. Build notified immediately with saved failed
log. This is a recoverable workflow-provider restriction, not a C++/toolchain or
Windows gameplay failure. Initial gh lookup defaulted to upstream vcmi/vcmi and
404ed; explicit `-R thegandalf196/vcmi` succeeded—no authentication blocker claim.

## Fresh MSVC successor preflight — SQLite notice failure, repair scoped

Independent API/log inspection confirms34071259251/head1a441500 completed FAILURE
in dependency-source/notice preflight, before compilation. Fatal message names
sqlite3/3.53.4#89fcf5cda598966acb7f3e185b19c58d; earlier GNU-mirror retries were not
the final error. No duplicate CI run was started by Tester.

The actual official SQLite3530400 amalgamation independently downloads with SHA
`1e71ddf93849c6a6ecf58b827c0692073d2dd7ee40196158068f7b29f422e87d`.
Its original sqlite3.h leading comment is1484 bytes, SHA
`cc65d76f325d884dd49e319acf3764d5513920f34ad18e34347b8dedc6ddd2ec`.
The dedication is embedded rather than a LICENSE-named file. Independently reviewed
repair a722279c6 preserves that complete comment and original-header provenance.
Actual-header execution returns exact bytes; individually altering each of five
required phrases, or truncating the comment, rejects; restored input passes.
The offline notice suite independently passes13 tests/exit0. Evidence:
`testing/windows-audit034/{msvc-run34071259251-failed.log,sqlite-declaration-check.json,
sqlite-notice-tests.log,sqlite-notice-tests.exit}`. This corroborates the narrow
notice repair, not a completed cloud package. Independent API verification now
confirms source-only successor34073336547 at a722279c6 **SUCCESS**, artifact10001693338,
321860495 bytes, advertised digest
`db32dbc96c51198dcb28a4ecebc6e30810b1aab5d7cc71bbbc2d1bdd5f6a0216`.
Actual downloaded artifact inspection remains separate from API/run success.

## Old MSVC profile-path classification — known public CI paths, not personal

Independent full old82 repaired-ZIP inspection reproduces9 PEs/191 ASCII profile
hits. Every hit normalizes to `c:/users/runneradmin/.conan2/`; no other profile,
UTF16 profile (both alignments) or Linux home-path hit was found in the checked PEs.
All seven affected vendor DLLs (169 hits) exactly match the public pinned cache
`6772d2e9...`; the remaining22 client/library hits are unchanged from the original
cloud82 download. Independent API re-verifies original34005089136/source82 success.
The repaired ZIP hash remains `5c41caa19...`, untouched.

**Classification:** these are evidenced CI service-account Conan build paths,
not personal workstation information. A narrow provenance-conditioned policy is
justified: report the known CI classification/counts, require verified CI/cache
lineage and the exact generated Conan subtree, and keep unknown accounts/subtrees
or missing provenance blocking. Do not silently suppress arbitrary profile hits
or treat the username alone as proof. Local034's173 actual personal-path hits
remain RED. This does not approve a future feature ZIP or native Windows play.
Evidence `testing/windows-audit034/old-msvc-ci-path-classification.json` contains
per-image hashes/offsets and exact public-cache matches. Build owns policy/tests.

## Earlier prevention coverage audit — before a3 implementation

Read the new `NH_RELEASE_FAILURES.md` against actual workflow/collector code.
At that checkpoint the workflow invoked four Python suites (25 cases after SQLite repair), not
the whole relevant package set. Tester independently ran all ten offline suites:
**54/54 PASS, all exits0 on Linux**, covering binary privacy, media roots, runtime,
GNU sources, MinGW wrapper, curated resources, dependency notices, historical
notice overlay, PE audit and system-provider classification. No compilation or PE
execution. `testing/windows-audit034/package-regression-suite.json` records test
source hashes/exits/counts and per-suite logs. Windows symlink-sensitive fixtures
need appropriate target support, not silently skipped coverage.

Actual34071259251 full log additionally proves native PowerShell5.1 Setup's50
synthetic checks and Managed-Preset smoke both passed before SQLite preflight
failed. This closes that prior default-parameter smoke failure at target-shell
scope, not CMD entrypoint, real assets, game launch or gameplay.

Concrete automation gaps sent to Build: current source-only artifact upload has
implicit success-only gating; source errors lack a structured per-node collected
report (missing-notice aggregation alone is narrower); whole-graph reporting must
explicitly justify unshipped Qt exclusion rather than silently equate it with an
OS provider. Required successor checks include independent multiple safe failures,
missing/altered source, immediate unsafe-extraction abort, retained sanitized
failure report, graph-drift rejection and overall nonzero until all required rows
pass. An independent synthetic two-error probe confirms the current collector
stops at a safe missing-recipe failure before inspecting the second dependency's
missing notice; fail-closed rejection works, requested aggregation/reporting does
not yet exist. `prevention-coverage-probe.json` records it without Conan/network
execution. These are requested next gates, not a claim the automation exists.

## New prevention implementation and a0 repair — independently scoped

Build implemented full-suite dispatch, per-node safe-error collection, explicit
Qt/preset exclusion evidence, and always-uploaded sanitized reports at a3c96f5c2.
Tester found a synthetic CI-path escape: truncating a space-bearing path before
traversal validation incorrectly approved an outside-subtree path. Real191 CI
hits were unaffected. a0dcdd92f fixes full-NUL-string validation; independent nine
space/tab/quote ASCII/UTF16-alignment controls now reject. Actual old191 remain
reported CI-classified; actual local173 remain blocked. No frozen artifact changed.

The actual collector plus new preflight independently retains two safe failures
(missing recipe and missing implementation notice), then checks a third passing
node. An actual escaping-link guard aborts before the next node. Reports retain
statuses without private input paths; temporary node inputs are removed. No Conan
or network execution in these synthetic probes.67 cases across11 offline suites
independently pass on Linux. Build/Dispatcher's full72 report is separate; Tester
did not invoke the five tests that create synthetic Git commits. Actual source
archives were independently checked against existing pinned Git objects instead.
Evidence `ci-policy-a0-independent.json`, `preflight-independent-integration.json`,
`a0-offline-regressions.json` under `testing/windows-audit034/`.

Independent API confirms34076215259/a3 failed the71-test regression step;
client compilation was SKIPPED. Windows case-insensitive storage could not create
the collision intended by a fixture, so it reached the optional PE parser. The
repair supplies a virtual colliding directory listing and asserts parsing is never
called; it is not a pefile-install workaround or weaker production check. Build
started full successor34076721466/a0 without repack/duplicate. Independent API now
confirms target72, PowerShell5.1 and full source preflight SUCCESS before client
compilation. Actual downloaded report artifact10002744289 matches advertised SHA
`ff8602da6adeee18305f3f75a98c9536579d1d242b43427e09551718d7ea3ec5`.
It records72 tests/zero failures/errors/skips, clean tracked tree,76 graph rows and
32 passed host nodes. Refs/IDs/revisions match actual a722 source inventory; Qt
exclusion is tied to the exact disabled-tools preset. All nine cached CI-reported
images/186 hits match public-cache bytes; this includes unshipped OpenSSL modules,
so must not be confused with old shipped191. No unapproved hits. Evidence
`a0-target-preflight-independent.json`. Latest independent API checkpoint: actual MSVC compilation **SUCCESS**;
packaging/recursive PE/source step **IN PROGRESS**. Its final artifact remains
separate from the accepted/published local MinGW archive above.

## Actual a722 source-only artifact bytes — independently checked

Frozen extracted artifact10001693338 has32 dependency records matching the pinned
public cache's exact refs/package IDs/revisions/settings/options. Its113700-member
source tar covers31 implementation roots plus a separate system-only provider.
All31 exact recipe manifests and78 recipe/export checksums match;14 contained links
are checked.188 source notice files match archive bytes and29 package notice files
match the exact public cache. SQLite's derived notice
matches the original complete1484-byte leading comment and all four implementation
files match the independently downloaded official archive.

Fork source contains4537 files from a722 and four pinned submodules:596 exact Git
blobs and3941 matches after **CRLF-only Windows checkout normalization**, with
exactly15 reviewed exclusions. This is not a byte-identical LF source-export claim.
Dependency tar SHA `4ac2979db84ab01e16b7c9931e0cc3fc590f33e3cb3fa15a1ee853c6d933ea25`;
fork tar SHA `427f78bf7ff4ff61f773ed8c615bf1af3f15916f9d31237feec783666c2dfec2`.
`source-preflight-artifact-check.json`, `sqlite-preflight-cache-match.json` and
`source_preflight_check.py` retain evidence. Actual final Microsoft notices/feature
binaries/package execution are not established by this source-only artifact.

## Remapped local dependency freeze — privacy component pass, not final package

Independent inspection of `remap-deps-frozen/` verifies28 exact AMD64 PE hashes,
zero ASCII/UTF16 personal-profile hits without a CI exception, and988 intra-set
import/export matches. Of26 host nodes,25 package IDs changed; only system OpenGL
is unchanged. Exact graph SHA
`424274170fa5ff7f1f599736898de6c107b7dafaee50410dde8b3fd1d7e8f752`.
Four GNU runtime DLLs and the documented Ogg loader alias are outside this28-image
set and remain required at final deployment. Build is compiling separately from
frozen034 source, not the mutable future tree. Old173-hit ZIP remains unchanged/
rejected. `remap-deps-independent.json` records dependency-only scope; await the
actual new engine/deployment/package before extending acceptance. No GUI lease.

## Actual local replacement audit — packaging593e40b7f

Audited actual files in `windows82-local-repacked-593e40b7f`, not merely the
preflight overlay. Explicit packaging revision
`593e40b7f84b982e4e2ccf6833fc304b2a17b205`, local run
`windows82-notices-20260906`; compiled82/run34005089136 identities remain unchanged.

| Actual archive | Bytes | SHA-256 |
|---|---:|---|
| Play ZIP |23739575|`5c41caa19ed75df8d1f572fea575d5027ceb61e58376cfc1aaaa308fe5792644`|
| Original fork source |16101678|`4c33a98794748bae525e4d0add92090221f1c9f71726b266e6b9af1f7d203d6f`|
| Repaired dependency sources |288811062|`f0e06ced467a00e9bb3985e6f76b37dcf64f6355d332cde6238fb42b26880c98`|
| Packaging source |14790620|`67fb6f1eddd3f525fef59913af85508b308dd69816ace0864b77ab1d32ee074b`|

Independent checks pass:

- Outer hashes, ZIP CRC/safe Windows names/no collisions, links or encryption;
  763 files, all762 internal manifest entries exhaustive. No original file removed.
  Only BUILD-IDENTITY, DEPENDENCIES, SOURCE-NOTICE and checksum manifest changed;
  five additions are repack provenance and four license texts. All725 protected
  payload-map hashes match, and every unchanged old file was compared directly.
  Thus all26 PE binaries, imports report,683 resources, helpers and old notices
  retain their previously audited bytes, not merely equivalent versions.
- New27-entry inventory matches original static references/package IDs/revisions,
  settings/options. Dav1d BSD-2-Clause, Brotli/PlutoVG MIT and complete FFmpeg
  LGPL2.1 notice bytes match the supplied corresponding sources. Original notices
  remain intact. Separate local packaging identity does not imply a new engine build.
- Repaired dependency archive's decompressed tar bytes equal the independently
  checked overlay:111782 original entries preserved plus590 additions. Its gzip
  hash differs from the earlier preflight container; final hash above is authority.
  Seven added recipe/data/patch files match the exact original prebuilt cache.
- Independently retrieved the three small public upstream source archives, verified
  all three original conandata SHA-256 pins, and exhaustively compared **529 source
  files**: no missing, extra or differing bytes. Required Brotli patch is separately
  present and matches the original cached export. No duplicate prebuilt download.
- All3786 packaging-source files match Git593; only `android/gradlew.bat` requires
  CRLF normalization. Exactly13 reviewed encrypted-credential/worker-diary files
  are omitted. Optional gitlink trees are not packaged here; the unchanged full
  compiled82 source companion remains separate. Packaging scripts are present as
  matching Git blobs, not replaced by current dirty worktree files.
- Changed/added player-payload text has no tested personal home-path hits. New
  upstream sources are hash-pinned public content; original privacy/provenance
  findings and limitations remain applicable. No installation or binary/GUI
  execution occurred during this audit.

Evidence under ignored `testing/windows-audit-repack593/`: `archive-checks.json`,
`packaging-git-check.json`, `source-license-checks.json`, pinned `upstream/`
inputs and `upstream-comparison.json`.

**Verdict: PASS for this local original82 notice/source repair.** All four known
notice/source omissions are repaired. This is not publication approval, exhaustive
rights clearance, native Windows runtime acceptance, or distribution acceptance
for the later commands/six-school/growth/mastery/tier implementation.

## Required evidence from Build

- Exact downloadable GitHub run/artifact or release URL, source commit and build
  configuration/toolchain/target Windows minimum. Distinguish source/evidence
  commits from the commit actually compiled into the binaries.
- Archive SHA-256, size and complete file manifest; dependency versions/provenance,
  build-option record, and corresponding-source download for the distributed build.
- Audit the **downloaded archive**, not only the staging folder or workflow text.
  Record inspected archive identity here before assigning PASS/FAIL.

## Archive gates

1. **Safe, ordinary extraction.** ZIP opens without errors; no absolute paths,
   traversal, drive/UNC paths, NTFS alternate streams, unsafe links, duplicate or
   case-colliding names. One clearly documented launch root; no accidental nested
   artifact wrapper that conceals the playable ZIP. Windows filenames/path lengths
   must permit ordinary Explorer extraction to a user-writable directory.
2. **Runnable x64 payload.** `VCMI_client.exe` and `VCMI_lib.dll` must be genuine
   AMD64 PE files from the same build. Recursively inspect normal and delay-load
   imports: each dependency must be supplied with compatible architecture or be a
   documented Windows system component. Include required SDL/media/Lua/TBB/other
   enabled runtimes and app-local redistributable runtime components as applicable;
   do not rely on a CI runner's Visual Studio, Conan or globally installed DLLs.
   Debug-runtime requirements or mandatory admin redist installation fail the
   no-admin portable requirement. A static import check cannot prove dynamic load
   paths or runtime initialization; retain that limitation.
3. **Internal authority and ordinary AI.** Build evidence must show the client
   links its authoritative simulation and uses the internal single-player route;
   no separate server executable is required/shipped for this curated preview.
   `VCMI_lib.dll` is the facade containing core, Lua and enabled AI—not a requirement
   to ship separate AI DLLs. Verify ordinary Nullkiller2/BattleAI/StupidAI build
   settings and default configuration, not merely filenames. No tests, synthetic
   maps, test executables, benchmark tools, compiler caches, PDB/debug packages,
   standalone lobby/server, or unrelated editor/launcher payload in the play ZIP.
4. **Complete open engine resources.** Match `config/`, `scripts/`, and
   `Mods/vcmi/` against the declared source revision/curated installation manifest;
   checking three directory names is insufficient. Include JSON schemas/defaults,
   AI configuration, Lua scripts and essential mod content/dependencies. Verify
   referenced files and preserve required original-compatible handlers. No optional
   third-party mod installation flow is needed for this edition.
5. **Licensing and source.** Include VCMI GPL license and preserved attribution,
   upstream/embedded resource notices and licenses for each distributed dependency.
   A CPack license setting is not evidence that a ZIP contains the license text.
   Provide corresponding source for this fork/build, including build/packaging
   scripts and applicable dependency source obligations; an upstream-only link or
   binary-only release is insufficient. Verify supplied source URLs are reachable
   and match the recorded versions. Naming/distribution-rights review is separate.
6. **No purchaser/private content.** Inventory every member and compare against
   tracked open resources and documented dependency payloads. Exclude original
   Heroes executables/installers, LOD/SND/VID archives, purchaser maps/campaigns,
   music, saves, screenshots, logs, profiles and local settings. Do not blindly
   reject all images/audio: legitimate essential VCMI resources require provenance.
   Inspect text plus ASCII/UTF-16 binary strings for credentials, tokens, keys,
   personal usernames/home paths and debug/source paths; report findings privately,
   never paste secrets into committed evidence. Extension/string scans alone cannot
   prove absence—combine with complete inventory/provenance and clean CI staging.

## Extract → select assets → play review

- A non-developer downloads the named Windows x64 play ZIP (not GitHub's source
  ZIP), extracts it fully to a writable folder, then opens the documented entry.
- No Git, Python, Conan, CMake, SDK, administrator prompt, developer-mode symlinks,
  machine-wide execution-policy change or security-software disabling is required.
  State unsigned-preview warnings honestly; do not recommend blanket bypasses.
- Select an existing purchaser-owned Heroes III Complete directory containing
  `Data`, `Maps`, `Mp3`; distinguish Complete from the incompatible HD edition.
  No original executable launch/download is needed. Explain any private local
  copying, storage cost, and exact write locations; never modify source assets.
- Quote and safely handle paths containing spaces/non-ASCII/shell metacharacters.
  Cancellation, missing/incorrect assets, unwritable destination and incomplete
  archive produce visible actionable errors, not an unexplained disappearing window.
- Keep saves/config/logs isolated from upstream VCMI and Linux/manual profiles.
  Document their location, repeat launch, asset reselection, upgrade and removal
  behavior without deleting user saves. Never package generated personal paths.
- Ordinary menu → New Game → Single Scenario → Begin, or Load Game; no host,
  address, port, optional mod-store or separate server setup instructions.

## Later Windows runtime gate (not performed)

On an explicitly authorized real Windows x64 system: test clean non-admin launch
without build tools, asset selection, original scenario start, movement/town/turn,
combat, save/quit/relaunch/load, errors/cancellation, and isolated profile writes.
Observe exact process identity/children and owned TCP/UDP independently. Record
Windows version, archive hash, evidence windows and limits; do not upgrade a
packaging PASS to gameplay PASS before this occurs.

## Source inspection findings before artifact delivery

- Root `CMakeLists.txt` installs scripts/config/Mods, and calls Conan runtime
  installation. Audit actual staged/install output for omissions and surplus files.
- `libFacade/CMakeLists.txt` names the Windows facade `VCMI_lib.dll` and absorbs
  enabled AI/Lua objects; absent separate AI DLLs is expected.
- Upstream Windows installation documentation describes a Qt launcher, optional
  recommended mods and a shared Documents profile. That is **not** the curated
  preview's user contract. Upstream developer build/admin instructions must not
  become the player's setup guide.

## Actual downloaded artifact audit — 2026-09-06

Run `34005089136` succeeded, artifact `9981596987`, source
`82a4e00b29485cb068a43616363bd26c3ab3844b`. GitHub reports an unexpired
326168082-byte artifact. Audited Build's downloaded four files, not CI staging;
only the play ZIP was extracted, into ignored Tester evidence storage. No GUI,
installation, binary execution or preserved-preview/profile writes occurred.

All three distributed archive checksums match `SHA256SUMS.txt`:

| Archive suffix after `New-Horizons-Windows-x64-82a4e00b2948` | Bytes | SHA-256 |
|---|---:|---|
|`.zip`|23687246|`8ca4668fb5d98ced4314ce019583430acd47ffb610e73691266fb7526a0306c1`|
|`-source.tar.gz`|16101678|`4c33a98794748bae525e4d0add92090221f1c9f71726b266e6b9af1f7d203d6f`|
|`-dependency-sources.tar.gz`|286378071|`84bf98933c79f4ac7ce8a4d6b52505b140f68d3c4a7c9fa78f9bd6ede77fc1e8`|

Passed inspected gates:

- ZIP CRC, safe names, no links/case collisions, one launch root:758 files,
  73503783 uncompressed bytes. All757 manifest-listed payload hashes match;
  manifest coverage is exhaustive except its own checksum file. Longest path
  below launch root is103 characters; use a reasonably short extraction path.
- Independent PE parser:26 AMD64 binaries, including one client and25 DLLs.
  Normal/delay/forwarded imports agree with `PE-IMPORTS.json`; non-bundled imports
  are Windows system/API components, not debug CRTs. Static closure does not
  prove dynamic initialization or codec playback.
- Client SHA-256 `7a87fa6497bf6bf71c2603fee295cab83c8d5598edd38c043c5f60f12cfeed09`;
  facade `c04a846a0aa74850a73fcb92936bbc4e09a9740326a328b37f40881079096822`.
  Build identity records SDL3, MSVC19.29, ordinary AI enabled, standalone server,
  tests and performance experiments OFF. No server/test/editor executable ships.
- All683 engine resources match the declared Git tree, allowing only checkout
  CRLF normalization and the intentional Windows `config/dirs.json` override.
  The three player helpers match that revision; setup tests are not shipped.
- Both source archives pass lexical path/link containment checks:4071 fork
  members;111782 dependency members,12 contained links. All23 listed non-system
  dependencies have source roots/recipes, but the list is incomplete (below).
- Root GPL/attribution, xBRZ terms and Microsoft terms are present. Both pinned
  Microsoft DOCX hashes and OOXML integrity match their provenance records.
- No purchaser archives/maps/saves, profiles, logs, test payload or PDBs found in
  the play ZIP. Targeted private-path/token checks found no player-payload hits.
  Source-only scanner positives were reviewed: two Boost CVE synthetic strings
  and tracked macOS PSD raster bytes are not credentials. Corresponding source
  retains the exact upstream `TerrainViewTest.h3m` test fixture, not a purchaser
  map; it is absent from the play ZIP. Generic CI Conan paths are embedded in
  FFmpeg; these are not purchaser identity. Scans are not exhaustive secret proof.

**Publication blocker:** `MEDIA-RUNTIME.json` records static
`dav1d/1.5.4#001b758cfd88fd816b286e09e686dd5c`, but `DEPENDENCIES.json`, bundled
notices and dependency-source roots omit it. Actual `avcodec-63.dll` contains the
VideoLAN dav1d decoder and configure flags enabling libdav1d, so this is not just
an unused recipe option. Add exact corresponding notices/attribution and inventory
(and source for the promised complete dependency bundle), then re-audit the
replacement archives/hashes. Check other skipped static transitive dependencies
rather than treating PE closure as license closure. Runtime/Build were notified.

Also requested: copy FFmpeg's applicable full LGPL text into its play-ZIP notice
folder; that folder currently contains only `LICENSE.md`, which refers readers to
`COPYING.LGPLv2.1`. Source-companion terms are not missing merely because this
play-ZIP folder is incomplete; retain this distinction when repairing notices.

Local derived evidence: `testing/windows-audit-82/download-checks.json`,
`independent-pe.json` and `source-audit.json` under the Linux build root.

### Local repair preflight — additional static-transitive coverage gaps

Build proposes retaining original82 binaries/resources/helpers and all23 existing
non-system source roots/notices, adding exact dav1d and FFmpeg's full LGPL text,
with separate local repair identity. This is acceptable as a repair approach,
not a replacement-archive PASS or native Windows claim. Qt/md4c are not in the
original24-entry inventory (23 non-system plus OpenGL system); an unrelated
expanded collector graph must not redefine the original shipped build.

A renewed independent review found **two additional unresolved coverage gaps**:

- Actual original `plutosvg_0.0.7/conanfile.py` unconditionally requires
  `plutovg/[>=1.0.0 <2]`, with transitive headers/libs. Original inventory declares
  static PlutoSVG with FreeType enabled, but no PlutoVG source root/notice entry.
- Actual original FreeType recipe requires `brotli/1.1.0` when `with_brotli` is
  enabled; original inventory explicitly records that option True and static
  FreeType. Brotli is likewise absent from the inventory/source roots.

These are concrete declared-source/dependency closure gaps; exact original graph
revisions and inclusion in the shipped binary need confirmation. No PlutoVG or
Brotli ASCII names were found in the DLL scan; this does **not** establish absence
of static code. SDL3_ttf contains `svg-hooks`, consistent with SVG support but not
standalone proof of a particular implementation. Do not invent resolved versions
or certify these omissions harmless. Build was alerted before local repacking.
Local `static-transitive-review.json` preserves the actual two archive recipes and
hashes. Audit all original host static transitives, not just MEDIA-RUNTIME; its
only unmatched reference is dav1d, but it is not a complete dependency graph.
Subsequent extraction/review of all23 top-level archive recipes' requirement and
configuration methods found no further enabled external package omission beyond
these three under the recorded Windows options. Disabled codec/Harfbuzz/hwloc/
Unix backends must not be counted as required payload. Exact missing-package
revisions and their own transitive closure are still pending. Local
`recipe-closure-review.json` records the reviewed blocks and source hashes; this
is source/options review, not a recovered original Conan resolution graph.

### Original prebuilt identity independently recovered

Independently hashed Build's111081420-byte downloaded prebuilt archive: SHA-256
`6772d2e9f0a730329a195edce895a863d9115c4bba43101a9a6df3fefe6cfe43`
exactly matches original82 BUILD-IDENTITY. Read its actual embedded pkglist (not
only Build's derived report) and compared six contained DLLs byte-for-byte with
the audited play ZIP: SDL3_ttf and all five FFmpeg DLLs match. All24 original
package/reference/revision tuples and23 non-system settings maps match.

The actual SDL_ttf full static requirements establish:
- Brotli1.1.0 recipe`3f631ef77008f7b5eb388780116371a3`,
  package`ec8b0cadaef08ac51b3d13ed30e9881dd0c495ad`,
  package revision`b305b96810d98a808c094c6b1812796d`.
- PlutoVG1.3.3 recipe`95e0e6c6c8525ffc0652c4d1198811ae`,
  package`41ad450120fdab2266b1185a967d298f7ae52595`,
  package revision`fe7914e08df7e092bd23856f203d3670`.
- FFmpeg similarly identifies dav1d1.5.4 recipe already recorded above,
  package`bfe64efb343de717b80529c17cde303b98dcd697`,
  package revision`63381424f3ab1b20ec68e8ce4b16bb23`.

Walking pkglist requirements reaches27 entries: the original24 plus precisely
these three; their own recorded package requires lists are empty. Full references
were honored; weak ABI references to existing dependencies use the independently
matched original package tuples, not arbitrary other prebuilt variants. This
confirms original static dependency identities, **not repaired source/notice
coverage**. Local `independent-prebuilt-closure.json` records the checks. No extra
download, extraction/install or Windows execution was performed by Tester.

Prepared dependency-source companion independently compared exhaustively with
the original archive: all111782 old member payload hashes, types, modes, sizes
and link targets remain identical;590 new members are regular files/directories
under exactly dav1d1.5.4, Brotli1.1.0 and PlutoVG1.3.3 roots. Prepared archive
SHA-256 `e75518b0696edd81a86331f49fdab9f69554e4171bdb257d22ee83cb06542810`.
Local `source-overlay-preflight.json` records this component check. It is **not**
replacement play-ZIP acceptance, exhaustive new-source provenance validation or
publication approval; final replacement bytes/notices/manifests remain pending.

Final archive verdict: **FAIL publication gate pending notice/source repair.**
This is a packaging verdict, not a finding that Windows gameplay fails. Actual
Windows launch/gameplay/media behavior remains **UNVERIFIED**.
