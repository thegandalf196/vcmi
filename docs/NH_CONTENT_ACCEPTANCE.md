# New Horizons — content and ordinary-game acceptance

## Latest ordinary category GUI — bounded labels/Core-help evidence

Actual private950/980d fastsetup reached ordinary world:20Pixies/Core,
20AirElementals/Elite,2Phoenixes/Champion,100Pikemen/no category row. Core left-help
resolved classification-only wording and saved source new-horizons:creatureCategories
(version1). Elite/Champion help and minimum viewport remain open; observed
1280x800 at110% UI scale only. Installed/world rules agree here, so no conflicting
saved-authority or legacy-mapped fallback proof. No named save/restart/AI gameplay.
Exit227.826/session228.019 missed200target but met250mandatory/300hard;449clean
samples/full950/37/Data/map/control fences/complete owned teardown, no saves.
First setup-cutoff RED remains intact. Evidence `category060-world-gui-fastsetup/`.
Subsequent strict-interior NAV5374 actually passed all four clicks and keys1..4:
Tester viewed all8 screenshots; corresponding bodies pixel-exact and four distinct
contents. Growth no extra chances; Leadership100/900+100%; Siege Experts/base-only
Ballista x4/three100% controls and restrictions; Precision ACTIVE distance/wall-only
with remaining modifiers/targeting restrictions. Gold selection moves while mouse
hover stays white on Masteries for keys1..3. All normal text fits shown viewport;
no covered-key/overflow/minimum viewport or battle-effect acceptance. Client exit0
207.617s misses200target, meets250/300;409clean samples/full fences/teardown,
only original readonly5374 save. Earlier ROI refusal remains RED. Evidence:
`testing/category060-navigation5374-interior/summary.json`. Separate ordinary
battle entry later reached four stacks but refused cards on an incorrect gear ROI.
Distinct `category060-battle4-actual-layout/` then actually rendered/read all four
battle cards through observed friendly queue portraits:20Pixies/Core,
20AirElementals/Elite,2Phoenixes/Champion,100Pikemen/no category row. Separate
mandatory live quit-gear binding reached actual Battle Options. No further input
was dispatched after110.570s; forced exit124 at284.308/session284.730 is a journey
RED (200target/250normal FAIL, hard300 clock only PASS). Complete owned PID/X/socket/
lock removal and950/immutable37/Data/map/control fences verified;561clean samples,
no saves. All images reviewed offline after teardown. This supports bounded battle
card labels only, NOT normal-exit, context-conflict, effect/AI/full-save acceptance.
Scheduling-gap cause is unproved; original helpers/events and failed teardown
assertion retained. No automatic retry or deadline waiver.

## External master01 and future navigation — bounded independent review

External master `6dd6e0880acd84ea81f073a3c1030dadde0340d0c877d020e4cac502bc0e0d50`
and sidecar `3eb0bb1d3ff1d2780dc7c3c3d62e5ce9b400a414f43e85db8f13c5c94c71a658`
remain unchanged. Actually viewed the1254-square master and Artist's native sheet;
independently verified all four output hashes, exact full-square premultiplied
Lanczos32/44 reductions,82-square at(0,5) in82x93, and actual sheet pixel regions.
**Retained independent assumption RED:** original PNG contains `caBX`23617bytes
although Pillow metadata dictionary is empty. Original is NOT metadata-free;
four derivatives contain only IHDR/IDAT/IEND. No C2PA signature, tool history,
no-pixel-reuse claim or rights clearance verified. Supplied image_gen provenance
is a report, not witnessed generation; procedural CC0 does not automatically apply.
Ring/core survive native size; engraving merges and blue arc is faint at32.
Opaque32/44 squares are not full runtime alpha acceptance. No user style/subject
approval, family approval, proprietary-reference import or live import. Current
feedback concerns this external master, never a stale V5 choice; definitive art
is not a prerequisite for continuing engineering. Latest user direction permits
background/framing correction first, retaining the armillary, not final approval.
Tester actually viewed the private1026x355 original-skill comparison and read
updated brief/REVISION02/exact external edit handoff: smaller/darker instrument
with scene/blue cloth visibly differs from larger/brighter subjects on warm brown
fields. Original6dd6/sidecar3eb0 rechecked unchanged. No local rough mask, render,
upload, image-generation or import performed; Dispatcher routing is reported,
not at that point independently witnessed revision delivery.

Revision02 subsequently ACTUALLY received/viewed:1254RGB c3bbd23c…/sidecarb7139af5…
and private original-skill comparison/native32. Brown field replaces cloth/scene;
brighter/larger instrument addresses background direction, meaningful silhouette
fits.32px engraving still merges/arc subtle. Independent byte/CRC checks retain
caBX21844 despite empty Pillow.info; exact in-memory32/44/82 premultipliedLanczos
reductions match supplied clean derivatives. Original6dd6/3eb0 historical controls
unchanged (initial no-op sidecar placeholders explicitly corrected by actual
historical-to-current hash comparison). Sidecar image_gen report is NOT witnessed
tool history/rights/no-pixel-reuse proof. Tester observations routed to Dispatcher;
actual user quality/family approval still pending, no live import/generation/export.
Actual user feedback subsequently received: revision02 is "almost there" but
"The color of the leather background is not there yet" — NOT final approval.
Read external `REVISION_03_BACKGROUND_COLOR.md`: background-tone-only refinement;
"too orange/golden" versus muted slightly gray-brown is Dispatcher interpretation,
not an additional user quotation. Preserve instrument/texture/framing/light/core/
wisps; no whole-image recolor or proprietary pixel copying. User must submit the
prompt to the external editor; no automatic dispatch/monitoring or actual03 arrival
claimed. Await actual03/user feedback without further02 technical ACK loops;
product validation continues independently.
`sorcery-revision02-{handoff-,}independent.json` binds both historical handoff and
actual arrival review; these are distinct, not retroactive delivery acceptance.

Safe sheet: `build/new-horizons-linux/research/sorcery-art/external-master-01/native-review.png`
SHA256 `19772ab75b97c28f4795a2bd40b3adb25ac35828ae5a97067e70d62c286921dd`.
Evidence: `testing/commands-static/sorcery-external01-independent.{json,log}`
under `build/new-horizons-linux/`; only Tester reports were written.

Future navigation four-file offer is separate from413930 and both947 candidates.
Independent source review found `CSlider::setValue(0)` ignores its argument;
Frontend retained the first offer and repaired only its caller to `scrollTo(0)`.
Repaired manifest `research/hero-navigation/source-ready-scroll-fixed.json` binds
all four current files: exact one-call CPP delta, headers unchanged, added
multiple-available FIRST-fallback tests. Bounded source/lifecycle/declared geometry
review passes. Subsequently inspected producer pure-test/object compile exits0
and actual object-build log; independently executed the pure state binary0 with
both new FIRST-fallback assertion strings present and source hashes matching.
`hero-navigation-native-independent.json` binds that binary; no linked widget,
rendering, interaction or413 acceptance is implied. Refresh retains selected
available TAB, not scroll. Actual overflow-to-overflow thumb/text reset,
shrink/recreate slider, disabled/covered keyboard input, font fit and refresh remain
future gates. Evidence: `testing/commands-static/hero-navigation-source-independent.json`.

## Historical sealed19 source review — subsequent native checkpoint below

`category-context-source-review19` independently matches all19 hashes, readonly
single-link files; complete new implementation/tests and existing-file deltas
against413930 were inspected. Copied typed world/battle rules, canonical all-row
capture, temporary validated current-load assignment, loading-only old-absence
clearing and actual-battle proxy forwarding are present in source. No category
inference from tier/upgrade/capacity or gameplay mutation found in inspected code.
The14-row Conflux contract is a proposal; Firebird Champion explicitly provisional,
three fixture rows test-only. Neither roster activation nor separate recruitment
choices are accepted by a metadata framework.

Nine primitive/binary plus nine context tests were read, NOT executed. Distinct
world/battle mappings and source identities exercise actual HypotheticBattle in
source, but a missing discriminator remains: malformed **current world/battle
wire** with structurally valid unknown creature row should reject before replacing
its prior category. Primitive unsupported-version rejection does not cover that
registry gate. Requested owner tests/mutation and a category-specific invalid-world
error assertion rather than accepting any initialization runtime_error. No new
compiler/GUI authorization or ABI mixing with frozen413 is inferred.
Runtime subsequently authored a separate two-case malformed-current-wire addon
and owner-run mutation plan; it is not executed evidence or part of sealed19.
Build's separate sealed9 data/registration offer was independently hash/mode
checked and its five offline tests passed in an isolated copied sandbox. Native
schema/integration still pending. A concrete test gap was demonstrated: removing
only the output containment check survives all five owner tests because the
outside live file already exists. An independent NONEXISTENT outside-build probe
rejects2 in original/restored code but writes/returns0 in the isolated mutation.
Production code is correct and untouched. The subsequent sealed9-fixed offer
changes only category pytest and wire-test CMake registration (seven files exact).
Independently repeated fixed five-test suite0, containment-only mutation1, exact
restore0. Both normal and `--check` subcases now discriminate: normal detects
unexpected creation; check mode requires the containment-specific diagnostic,
not merely another nonzero error. Test gap closed; native integration remains
pending. Evidence: `testing/category-build9-independent/` including initial and
fixed mutation logs plus `fixed-independent.json`.

Evidence: `testing/commands-static/category19-source-independent.json` and
`category19-existing-delta.diff` under `build/new-horizons-linux/`.

## Subsequent held36-fixed native checkpoint — bounded PASS

First full build failed on test-only nonexistent PIKEMAN constants and incomplete
obstacle/campaign types; original36/log remain. Reviewed repair2 preserved other34
and strengthened valid-ID guards. Fixed full build0, producer25/NAV and separate
world-only/battle-only guard negatives now audited: each removed one wrapper's
validation, only its matching case failed (three assertions), control passed;
exact source restoration/full-target rebuild/clean2 passed after each. Current36
and actual restored three ELF hashes verified; warnings remain qualified.

Tester independently reran Native25 (9 rules/5 schema/9 context/2 wire) and exact
registered NAV CTest under sole300s lease:25PASS0skip/0fail, NAV1/1, bothexit0,
1.068s, own copied profile and before/after36+binary identities exact, teardown
verified. This supersedes the historical unexecuted-test notes, not the provisional
roster boundary. No GUI/recruitment/tier-mechanics/full-world transaction or413
acceptance inferred. Broad regressions/private0.6 candidate still pending.
Evidence: `testing/commands-static/category-{world,battle}-negative-independent.json`
and `testing/category36-independent-rerun/` under the Linux build root.

## Future category creature-window UI2 — lifecycle source repair

Initial originating-battle-first / real-stack-world / bare-absent lookup was
correctly placed before preview fakeNode creation. Independent source review
found Commander refresh changed its target then rebuilt without recapturing the
category or resetting the category section. Initial CPP/header remain preserved
and hash-bound to the first offer; no executed GUI failure was claimed.

Repaired offer `research/creature-category-ui/source-ready-lifecycle-fixed.json`
independently matches CPP3bb66e86…/headerf5ed8631…. Shared private resolver now
clears the copied optional, runs in init before fakeNode and on Commander update
after the new target/before rebuild; categorySection resets with Main. Bounded
source approval for future combined36 only. Optional28px local section offsets
are accounted for, not proof of minimum-resolution/long-translation fit. Native
object/link, real saved-context/absent/bare/conflicting battle-world cases,
Commander mapped-to-unmapped refresh and rendered tooltip/layout remain open.
Evidence: `testing/commands-static/category-ui2-{initial,fixed}-independent.json`
and `category-ui2-lifecycle-fix.diff` under `build/new-horizons-linux/`.

## Current usability rights and Sorcery art boundaries

User confirms F8/F9 WORK; requested missing buttons and ability/status artwork,
not a save/load repair. Independent exact891 ZIP/source trace finds retained
callbacks but no shipped Undead icon mapping. User identifies VCMI Extras;
independent public API review at exact tree
`9229af99b03540df3f247dc7a570a30ef9000670` finds972 entries, non-truncated,
GitHub license metadata null and no conventional license/copying/copyright/
notice/readme/credits paths. **Redistribution clearance is not established**;
this absence is not permission or a complete legal verdict. No approval to import
Extras art/config or promise unchanged legal status. Use independently authored
original assets/existing callbacks unless exact applicable rights are obtained.
`testing/user-feedback-891/{static-triage.md,extras-rights-independent.json}` and
independent API/tree/config hashes retain evidence. No product imports by Tester.

Read full `NH_ARTIST_SORCERY.md` at the post-GUI safe checkpoint and acknowledged
Artist ownership: isolated Sorcery draft/export paths only, no parallel Tester
replacement work. Native80x60/compact68x51 bookmarks, transparent header top28,
all rank sizes and correct states require provenance, actual composed GUI review
and explicit user approval. Existing prototypes remain functional, not definitive.
Frontend owns other art/UI and reviewed live integration with Build. Only Tester
runs GUI. See `NH_TESTER_RESULTS.md` for actual first mastery925 GUI save and
remaining readback/restart/Gold/setup/deadline limitations; no active lease.

## Convenience native loader checkpoint — bounded PASS plus semantic RED

Independently read actual native handler probe/source and compared both
`convenience-bonus-fixed-*.json` reports/exits0:10 icons resolve including real
SPRITES fallback; all nature/propagation/descriptions equal baseline, actual
Magic Arrow immunity row unchanged (its icon remains empty). Initial filelist
registration10-empty-icon RED led to inline canonical patches plus root Content
mount; typed API compile errors/unprefixed existence assertion RED retained.
No compiled-client/GUI claim. Subtype/value priority and fragment mount checks
still pending. Evidence: `testing/convenience-art-independent/native-loader-independent.json`.

Independent semantic review found current NO_RETALIATION is temporary inability
of the afflicted unit to retaliate, NOT Naga/Vampire BLOCKS_RETALIATION. Enum70/99,
actual creature configs and existing English text confirm. NO_RETALIATION and
SIEGE_WEAPON descriptions are empty in these reports; CStackWindow1096 requires
nonempty description (individual bonus override possible). Therefore10 resolver
hits never prove10 visible traits. Frontend confirms correction to
BLOCKS_RETALIATION pending Build ACK/reseal; art/hook unchanged. Preserve prior
reports and do not weaken empty-description filter. Siege icon resolved only;
future real Naga/Vampire/Undead/living/GUI cases remain required.

Subsequent bounded source follow-up: corrected42 manifest actual hashes PASS,
only note f0c2bd43… changed/other41 unchanged. Current Build data now uses
BLOCKS_RETALIATION and adds exactly one original Siege Weapon description,
explicitly exempt from description identity; no other non-graphics fields changed.
Reviewed note/current mapping source, not a corrected native loading claim.
`retaliation-source-followup.json` binds this scope. Old native reports predate
this correction and must not silently substitute.

## Help-fixed947 — bounded actual usability GUI PASS with explicit gaps

New fa096c…947 exhaustively verified independently against original947 and
parent925. Only resourcefragment/identity/checksums change; samea766/31a/b770.
Newreadonly46 changes only two-prefixfragment, regression and authorizednote11d16;
43other files unchanged/current-source-exact. Real native GeneralText hover/help
English and both old-prefix rawhover negatives checked; independent4data tests0.
`convenience-art-independent/help-fixed-independent.json` retains exact identities.

Fresh guarded1280x800:191 GUI: actualEnglish QS/QL hover and rightclickhelp;
initial grayQL/noquickslot, then visibleQS -> safeoneWESTstep -> visibleQL and
normalconfirmation restore maplandmarks/hero position. ActualOrrin L1Knight XP0,
ExpertArtillery,15/20/5/10,mana10/10, fourstacks100/20/20/20 restored. MP1560/1560
visible AFTERrestore only: numericbefore/movedMP not captured, do not invent
numericdelta. Gold20700/20wood/20ore unchanged; startingrandombonus differs from
first947's20000/27wood/27ore, not an income change.

Allfour actualcards viewed: Vampire Flying/NoRetaliation/Undead icons+text;
Lord same plus genuine LifeDrain100%text; NagaNoRetaliation withoutUndead;
PikemanJoustingImmunity without falseUndead/NoRetaliation. LifeDrain and Jousting
iconcells remain blank, outside selectedtenmapping: broader presentation gap,
NOT universal icon coverage. No combat-effect proof inferred from cards.

671307-byte real quicksave ecb86e25fad4311784fd1675724902f8b8892022fc9cd11cac17d7f49033d3b3
retained readonly. SameprocessQL, not freshprocesssave acceptance. F8/F9 omitted;
no battle/endturn/AI/newXP/mastery. Quitbutton349.24s, confirm358.4s, normalexit
358.577s (before400 mandatory);707clean samples, five ownedprocesses absent,
:191socket+lockgone. Exactnew947before/after, old947/925/dee/e5/3maps andpublic891
anchors preserved. Inputrightup-with-XY ValueError and finalnormalshutdownXIO
retained; neither hidden as product failures nor used to resetclock. Owners
released after verified teardown. Evidence `testing/convenience947-help-fixed-gui/`
summary/teardown/manifests and01..11screens. NativeWindows/release/fullredesign,
freshsave continuation and broaderUI/effects remain open.

## First exact947 ordinary GUI — stopped on hint-prefix RED

Exact947/946 payload and3maps independently audited; parent925/e5 and public891
anchors preserved. Fresh600s lease was explicitly granted (not retroactive relief
for earlier450s REDs). Ordinary NHConvenienceArmy starts normally; both added
buttons visible, initial QL rendered gray with no quickslot; gold20000 and hero
15/20/5/10 visible. Actual QS mouse button creates671560-byte `Quicksave.vsgm1`,
SHAdee34173c79fee9e00d5d8bdca13a9530506e36dce2c8e34a4f20a2a291b678a.
Readonly control retained; no load/restoration claim yet.

Concrete GUI RED: hovering QS prints literal
`vcmi.adventureMap.quickSave.help.hover` in status bar. Source
InterfaceObjectConfigurable::readHintText307-308 appends hover/help to a STRING
prefix; fragment supplied leaf `.help`, although that leaf exists as a translation.
This exposes a consumer-contract gap missed by earlier source/data checks.
Both QS/QL prefixes need scoped data/doc/test repair and a NEW freeze; do not
mutate947. Previous native/provenance results remain bounded, not GUI acceptance.

Stopped progression as ordered: no movement/QL restoration/cards/F8F9/battle/
endturn performed. Actual Quit-to-Desktop242.18s, normalexit265.55s,523 clean
process samples; all five owned processes absent/:191socket+lock gone. Final
confirm input XIO during normal teardown retained separately from clientexit0.
All owners awakened/released after postflight. Evidence:
`testing/convenience947-gui-first/{summary.json,03-quick-save-tooltip-red.png}` and
`testing/convenience-art-independent/candidate947-independent.json`.
Existing891 F8/F9 remain user-confirmed working; this is not a save/load defect.

## Corrected convenience native data — independent bounded PASS

Compared latest `convenience-corrected-*`, `convenience-priority-*-typed` and
`convenience-disabled-fixed` JSON/exits0, not the superseded10-icon report.
Ten descriptor icons resolve with nonempty descriptions; only Siege description
changes, nature/propagation and NO_RETALIATION/MagicArrow controls remain exact.
Fragment absent baseline, present enabled, absent even when installed but disabled.
Actual predecessor oracle dependency preserves valueRepair32/subtypePrecision32;
subtype wins over value. Hook compile log0 is object-only, not linked client/GUI.

Remaining native gaps explicitly sent to Build: probe calls BTH directly, so
per-instance customIconPath precedence in CStackInstance165-169 is not exercised.
Unchanged empty MagicArrow result has no competing descriptor icons, so it is not
spell-priority stress. Earlier identifier-overload exit3 and missing mandatory
oraclemodType/legitimate ignored-dependent-mod failures remain retained. Evidence:
`testing/convenience-art-independent/corrected-native-priority-independent.json`.
Actual creature/button/portrait/save/load/spell-status GUI and Windows remain open.
Subsequent fullpriority probe closes the two bounded native gaps: actual
CStackInstance custom path precedes subtype/value and spell; three competing
SPELL_IMMUNITY graphics plus loaded sentinel do not override actualMagicArrow
specialempty icon, while custom does. Both contexts0/compile10=0 independently
checked; builds7/8/9 setup failures retained. `full-priority-independent.json`
records this, not arbitrary registry or GUI coverage. Corrected4 data tests also
independently0 before later GUI hint-prefix RED.

## Sorcery actual rendered V3/V4-keyfix proofs — technical review only

Actually viewed clean768 V3 master/native sheet and V4-keyfix master/native
comparison. Real volume/material distinction improves on rejected V2. V3 user
feedback says closer but too photorealistic; V4 uses larger key, grouped lighting
and procedural pigment fields, not manual painting/filter. Closed repaired bow
visibly complete, not a mesh-collision certificate. Grain/contact/material feel
and32px Sorcery semantics remain open user-quality questions; no motif/style/family
approval or live import inferred.

Inspected full V3 scene/reducer, V4 source diff and exactone-line key-transform
repair. Independently matched declared hashes, V3five/V4six metadata-free PNGs
(IHDR/IDAT/IEND only), cleanmaster/raw RGBA identity,44/32 reductions, centered82
square in82x93 and exactnativecomparison pixels. RawBlenderPNG/.blend/settings
contain private metadata and remain ignored; their hash presence is NOT permission
to publish. V3 prior independent manifest binding intact, firstV4 files consistent
with its retained manifest (no independent pre-render baseline claimed). Initial
metadata RED and firstV4 bow-intersection RED preserved. No independent rerender
repeatability or final runtimealpha/rank/full20asset/GUI acceptance.
Evidence: `commands-static/sorcery-{v3,v4-keyfix}-independent.json`.

## Sorcery V2 three studies — independent offline review only

Actually viewed all three520x225 native sheets under
`assets/new-horizons/sorcery-art/renaissance-studies-v2/{study,conjurer,manuscript}/`.
Distinct book/candle still life, hand lifting paper and cosmological folio; no
orb/shield repetition. Still conspicuously angular pixel planes, not finished
painterly modelling. Detail at32px is constrained; rank progression is unassigned.
At this inspection choice remained pending; user subsequently rejected V2 as
crude. Technical PASS is not aesthetic acceptance. Read the superseding
`docs/NH_SORCERY_STYLE_BRIEF.md`: miniature fantasy still-life paintings, modeled
materials/chiaroscuro and a high-resolution master reduced to44/32, not more flat
angular pixel batches or modern jewels. One credible quality proof first; no
approved motif/style guide/import. Tool availability is not an artistic guarantee,
and research history/proprietary reference rights are not independently established.

Independently checked15 PNG/SVG pairs against manifest hashes, dimensions/RGBA,
clear exterior rim and header top28; inspected full original Pillow geometry
exporter and SVG allowed primitives/no external references. CC0 provenance limits
reviewed; no reference pixels consumed by exporter. Initial edge RED retained;
geometry repair/export0 does not establish aesthetic acceptance. No independent
regeneration/rerasterization yet, no full20-family/native game GUI/import approval.
Evidence: `commands-static/sorcery-v2-independent.json`. A/B rejection remains.

## Sorcery user review supersedes worker preference

User viewed and **rejected both Meridian and Aperture** as too modern/Olden-Era-like.
Tester's earlier advisory A preference is not approval and is superseded. Preserve
both drafts as rejected references; no import. Read the updated charter's current
Renaissance-like exploratory direction: three distinct illustrative studies first,
not more full-family orb variants. User chooses before a style guide is established;
then full family/provenance/native-size/composed-GUI gates still apply. No approved
style definition or definitive art claim yet. Artist owns all replacement work;
Build arranges a bounded generation slot, only Tester uses game GUI.

## Separate convenience source/art review — offline PASS only

Actual Frontend42 hashes match, including repaired optional hook1f0a8bd3….
Independently reproduced all38 new outputs byte-exact (18 PNG/18 editable SVG/
2 animations); all421 old outputs remain exact against independently assembled
baseline. Re-ran structural/state audit and six negative controls, exit0; actual
native contact sheet viewed. Reviewed original geometric/Pillow exporter and CC0
provenance; no Extras asset/config import approved and no legal guarantee.

Optional hook builds base layout first and admits only fragment items in a
sanitized OBJECT, not addon dimensions/libraries; portrait remains excluded.
Initial bare-array Debug-assert source RED retained. Build fragment has namespaced
24x24 Quick Save/Load controls using existing callbacks/gates;10 core descriptor
patches alter graphics.icon only. Independent4 data tests exit0. Private0.5.1
composition is output-only; actual loading/merge/compiled hook/GUI/button state/
spell-status/layout/native Windows and final art-quality gates remain pending.
Evidence: `testing/convenience-art-independent/` reproduction/baseline/negative/
source-config reports and `commands-static/convenience-data-independent.log`.
This work is separate from held82 mastery/925 candidates and Artist Sorcery drafts.

## Linux891 public prerelease — independent post-publication PASS

https://github.com/thegandalf196/vcmi/releases/tag/nh-preview-89165787e50d-linux

Independent anonymous API checks bind all five actual asset sizes/digests to the
previously audited player, engine source, shared dependency source, tooling source
and checksums. Actual remote tag points tofe2a81f8c03937a12b00669cd77f59c2899b0c31;
compiled engine remains891. Retained actual anonymous public player is byte-identical
to audited55d97e… TAR. Public notes match reviewed draft (ignoring surrounding
whitespace), retaining exact-transfer/newest-save/NONE/native Windows/full-scope
limitations and licensing distinctions.

Actual public tag autoarchive4409 entries including root has4026 exact root Git
blobs plus committed `android/gradlew.bat` CRLF conversion;16 private exclusions,
four unpopulated submodules. It includes capability source/license but no future
NewHorizons mastery implementation; explicit source companions remain necessary.
Evidence `testing/linux-891-audit/publication_audit.py`, anonymous API/tag snapshots,
`publication-independent.{json,log}` exit0. No duplicate artifact downloads or
full redesign/native Windows completion inferred.

## Earlier Linux891 prerelease notes — review PASS, public bytes were pending

Reviewed actual draft `release-notes-891-linux.md`, SHA256
`5008ac8e481f0d2a08df40e8b295b420235013388ef12735f135d05652c96ed4`.
It distinguishes93-damage battle evidence from restoration of the prebattle1000
army, explicitly excludes unattempted exact-package transfer/newest Day2 restart,
NONE/native Windows/mastery/tier/new-effect/full-scope claims, and accurately explains
891 engine/fe2 packaging, shared c550-named source companion and component terms.
No remaining owned prepublication blocker for this bounded unsigned Ubuntu preview.
Reviewed bytes/report retained in `testing/linux-891-audit/`; actual public assets,
tag and notes still require independent post-publication verification. Windows
client-compilation progress is not a terminal package result.

## Exact Linux891/fe2 fresh-process restart/turn — scoped PASS

Separate600s/450s lease used packaged wrapper and fresh `testing/linux-891-reload/`
profile, normally copying protected874003d… **prebattle** Day1. Actual restoration
shows XP0,1000 Pikemen, Expert Artillery, both primary15/20/5/10 and capacity750/75%,
1170 movement; not the later battle's995 army/120XP. Ordinary owned-town travel
spends movement to688. Logged Blue NK2 turn then advances to Day2:1170 current
movement/1000 troops/mana10/10, gold20600→21100. Optional500-out/back transfer was
**not attempted** to preserve reload/turn/save/cleanup priority. No exact891 transfer
claim is substituted from private59 evidence.

Actual normally named save `profile/data/vcmi/Saves/NHCapabilitiesArmySiege
2026-09-07 1/Autosave-111-day2.vsgm1`,576785 bytes, SHA256
`6d7c00cc79b8caf3b8dc603fb55bee57122eb2ccabf9e7d927d0e518cdcfcbf0`,
plus equal read-only `main-day2-control.vsgm1`. Name comes from selected filename
plus typed suffix, not inference from automatic-save naming. **Day2 not restarted**.

Normal quit0 at422.442s, before450;833 samples/no children/owned INET, exact client/
Xvfb/socket/lock absent.1130/map/all historical controls and874 original/read-only/
loaded copy unchanged. Screens01–05, full raw log and manifests retained. Verified
TEARDOWNWAKE released lease. No battle/other-presence/mastery/tier scope expansion.

## Earlier exact Linux891/fe2 packaged GUI — scoped PASS, restart was pending

Separate600s/450s lease launched the **packaged Play wrapper**, fresh managed
`testing/linux-891-gui/profile`, unchanged1130-file final package and ordinary
ArmySiege/Gold. Actual development displays primary15/20/5/10,1000 creatures/cap750,
75%, mana10/10 and1170 movement. Priority distinct normal **prebattle** save:
`profile/data/vcmi/Saves/NEWGAME-891.vsgm1`,574792 bytes, SHA256
`874003d126329c2455ae9e6ec32be9c0744d7e76a9afeee343d44432f3b4507a`;
equal read-only `main-day1-control.vsgm1` retained. No postbattle save is claimed.

Then ordinary pursuit, manual Pikeman defend, normal Ballista autocombat permission
and Auto Combat yield actual CBattleAI valuations/authoritative shots. Eight Ballista
damage events13,12,13,10,12,9,13,11 total93; AI Hold the Line and Pikeman actions
finish the remaining27 HP. Victory120XP,12 Archer losses,5 Pikeman losses/995 survive.
These are actual exact-package actions, not imported private59 gameplay proof.

Quit initiated by408.6s, before450; cleanup-only afterward, normal exit0 at460.790s
within unchanged600s supervisor.709 process samples/no children/owned INET, exact
client/Xvfb/socket/lock absent;1130/map/38f8/8dc0/16406/historicals unchanged. Full
raw log, action excerpt, screenshots01–03 and manifests retained. Verified
TEARDOWNWAKE released lease. Actual874 save fresh-process restoration requires a
separate grant; no transfer/other-presence/mastery/tier or publication completion.

## Final Linux891/fe2 package — independent static PASS

Frozen `package-891-linux-fe2/New-Horizons-Linux-x64`:1130 readonly files/1129 sums;
manifest2a53c8f684d2a1840aeff9a7b509a5be9a7627ebb8863a2bd33733cf52c472b2,
identity01504bc2bb8f7c00c7e0f2de2aa3ded65ed4e0f6cca608d7dcf41c67473151c9.
Every raw891 file except identity/sums is preserved; all added launcher, terms,
header-boundary notices and sanitized evidence files are independently accounted
for against exact source records. Final privacy scan passes.

Player TAR19514784 bytes, SHA256
`55d97e539398ab0b82e2b1819f05988d047d125b72c7a0f035bd14deb756ebfa`, contains
exact1130 payload files with normalized ownership and matching executable modes.
All three companion sizes/hashes verified, including byte-identical previously
reviewed dependency-source v2 `25fb2447…` (not assumed from its c550 filename).
Tooling source16810310 bytes SHA256
`e0e0ef355014fb4bac761f32519e187041e84151180db93d26c55eaff06bcebd` matches
actualfe2 Git blobs/types/modes/four submodules/exclusions. Commitfe2a81f8c03937a12b00669cd77f59c2899b0c31
contains exactly the two reviewed BUILDING/README blobs.

Actual packaged wrapper `--verify-only` exits0 without creating the proposed
`testing/linux-891-gui/profile`; no game process was executed. Reports/scripts/logs
under `testing/linux-891-audit/`: final-payload, tooling-source, fe2-text-commit and
wrapper-preflight. Exact packaged GUI and publication remain pending separate
approval; no Windows/full redesign completion inferred.

## Frozen Linux891 raw payload/source/providers — bounded independent PASS

Readonly `linux-release-891-origin-candidate`:916 files/915 exhaustive sums,
manifest81b41d70023ca28d75bfa3870be3c94763bf5cf60db622b874289db63a46c200.
909 resources match actual891 Git blobs; launcher/license/attribution exact.
Clientfe3815e7… and facadecf0707db… are AMD64 ELF64 with `$ORIGIN`, zero independent
profile-path findings,1874/463 strong imports resolved across178 current-host nodes.
No ELF execution, optional dlopen/driver or other-distro proof from these checks.

Actual891 source archive16810473 bytes,
SHA256 `80764e5ee04214c7be69c7fcaaf2bf62215a6980f5a39e51a7461a2688f96e36`:
4594 exact Git blobs/types,116 executable modes, four pinned submodules and16
committed exclusions; future mastery source absent. Initial Build clone/ref/path/
alias setup REDs remain history, not erased by repaired build/install exits.

Independent actual891 Ninja dependency inventory yields4471 headers. **Every current
header hash**, path owner, binary/source package version and copyright hash matches
prior c550 evidence.176 external ELF bytes/edges also match exactly; recorded
external static/startup paths and implicit link libraries agree. Actual688 g++
gnu++20 commands and selected cache match891 provenance, with no explicit plugin/
LTO options. This establishes scoped provider/header/source-notice reuse, not a
static-library bit-rebuild, blanket license exception or uninspected final companion.
Prior GCC per-file exceptions, UAPI syscall note, patched Boost, oneTBB Apache2,
glibc startup/relink and xBRZ GPL3 combination limitations remain applicable.

Reviewed Linux BUILDING/README text changes use actual engine identity and generic
rebuild aliases, truthful0.4 capacity/saved-view scope and managed profile guidance.
Reviewed SHA256s: BUILDING3877899a5ddb96eb3cbad76136dfec7c6e2248b8e712c66aedb0887de1037431;
READMEe0fa31c3aec78573c630cef43179af4796f942fef5a7b3b027adfce51bfe0478.
`testing/linux-891-audit/` contains independent ELF/resource, source, dependency and
provider-reuse scripts/reports/logs (all exit0). Bounded raw/text approval sent Build.
Final wrapper/notices/companions/payload and exact packaged GUI remain separate;
no publication/native Windows/mastery/tier/full redesign approval.

## Future Artillery mastery artwork — independent offline PASS only

Independent isolated regeneration reproduces all421 outputs byte-for-byte. The
previous409 hashes match both current files and actual frozen891 Git blobs, not
just the producer's manifest. Exactly12 additions: three Runtime option keys
`artilleryVolley`, `artilleryPrecision`, `artilleryRepair`, each32/64 PNG plus SVG.
Six PNGs have correct dimensions/RGBA and transparent/opaque alpha extrema; six
SVGs contain only original geometric primitives, CC0 marking and no image/font/
external-link payloads. Visible fan-projectiles, target sight and wrench distinguish
the options; in-game fit and interaction remain untested. Independent blank,
opaque and wrong-size controls reject.

Generator SHA256 `3e916cb13b11545e037d3a225f20a08715341213b6aaef31e84d25139b2000b5`,
README `983979717a8d6e1f3287ee7abb5841315c53e1214da24e4bdbde23c37edd00e8`.
Evidence: `testing/commands-static/mastery_art_audit.py`,
`mastery-art-independent.{json,log}`, exit0. No product output modified; no GUI or
compiler invoked. These static CC0 original geometry icons are not final bespoke
illustrations, button-state sheets, implemented masteries, activation or a change
to891 releases. Canonical gameplay/schema/AI wiring requires separate acceptance.

## Published Linux c550 preview independently checked — full redesign still open

Public prerelease `nh-preview-c5503cc3515a-linux` independently verified through
anonymous API: all5 actual asset sizes/digests match the audited player, three
source companions and checksum file. Actual remote tag resolves to ba85, not the
compiled c550 engine revision. The retained anonymous player download is byte-exact
to the audited1128-file TAR. Actual release notes preserve Ubuntu26.04/system and
optional-media boundaries, NONE/native-Windows/full-design limits, newest Day2-save
not-restarted caveat, exact engine/tooling distinction and GPL3/component terms.

Actual tag autoarchive has4399 entries including its root (4398 excluding root):
4016 byte-exact root Git blobs plus `android/gradlew.bat` converted under committed
`*.bat text eol=crlf`. Four submodule contents are absent and16 export exclusions
match; the explicit tooling companion remains4584 exact blobs with submodules.
The initial strict autoarchive batch-file byte assertion is retained. Build's
earlier draft-by-tag404/premature verification message was corrected through its
release-list lookup; this does not replace the independent public checks now made.
No duplicate asset downloads by Tester. Evidence `linux-c550-audit/publication-independent.json`
and retained anonymous API/tag/notes/diagnostics. Publication is a bounded growth
preview checkpoint, not full Goal completion.

## Linux ba85 guarded restart PASS — bounded exact-package acceptance

Separate420s/330s lease used the same1128-file package and a fresh client process.
Before launch the actual Day1 `NEWGAME.vsgm1` f9adc018… was hashed and copied to a
read-only private control. Normal Load Game selected **NEWGAME**, not the autosave
or older45aae/f7d saves. Screenshots01/02 in `testing/linux-c550-reload/` visibly
restore level2/XP1000, base18/24/6/12, total20/24/6/12, last gains3/4/1/2, Axe and
spellbook, Basic Pathfinding/Sorcery Magic,150 Dendroids/20 Grand Elves, mana10/12
and movement877/1500. This closes restoration of the actual newly created save.

An ordinary Blue Nullkiller2 turn again recruited Ash and ended normally (retained
run log); Day2 showed mana11/12, movement1500/1500, unchanged growth and gold21200.
A distinct **NEWGAME-day2.vsgm1** was entered in the normal Save dialog and reported
saved successfully:521163 bytes,
SHA`ca18742ba547eb8fdfe0c92324da112574e29930addca65be797c51c0943d019`.
That newest Day2 file was not itself restarted. The original f9 control and its
preserved copy remain byte-identical; ordinary autosave111 changed during play
and is separately inventoried, not mistaken for the protected control.

Normal exit0 at298.321s;517 samples report no client children/owned INET sockets.
Exact client/Xvfb PIDs and display socket/lock are absent; all1128 package hashes
unchanged. Historical45aae/f7d control digests also remain unchanged. Before/after
profile/save manifests, screenshots and a copied-out full restart run log are
retained. The first phase's shared cache log was overwritten by this restart:
its prior digest, separate console/screenshots and recorded log-read evidence
remain, **not a retained full first-phase raw cache log**. This evidence limitation
is explicit. Both leases are released; no GUI process remains.

Together with the exact source/dependency/package audits, these phases pass the
bounded Ubuntu26.04 c550 growth preview package gate. They do not accept the full
redesign, future capabilities/masteries/tiers, all spells/AI breadth, driver/audio
coverage, native Windows gameplay or publication assets not yet inspected.

## Earlier Linux ba85 guarded GUI phase1 — restart was pending at that checkpoint

Exact packaged wrapper/c550 binaries ran on guarded private Xvfb:191. Initial
Tester setup created video settings before the managed-profile marker, so launcher
correctly refused the unmanaged profile without starting a client. This failure
and refused stale-display input are retained. Adding the marker to the newly owned
profile fixed setup; **the original600s deadline was not reset**. Progress cutoff
remained480s. No gameplay state or candidate bytes were edited.

Ordinary `NHHeroGrowthXP`, Red human/Blue computer, Gold start: Orrin picked up the
Axe, accepted the Seer's1000XP and visibly reached level2 with actual gains
+3/+4/+1/+2. Selected **Basic Sorcery Magic**, not Luck. Hero screenshot03 shows
XP1000, Axe/spellbook,150 Dendroids/20 Grand Elves. Development screenshot04 shows
base18/24/6/12, total20/24/6/12, last gains3/4/1/2, mana10/12 and movement877/1500.

**Save-name reconciliation:** the normal Save dialog retained its default name
`NEWGAME`; there is no file named `Level2`. Success dialog was observed. The actual
new checkpoint is `linux-c550-gui/profile/data/vcmi/Saves/NHHeroGrowthXP 2026-09-07 1/NEWGAME.vsgm1`,
519502 bytes, SHA`f9adc0187a2c81d6cb98e57794a61aab5f6b611e6a213c245a0c40c18c710bdb`.
It was created on Day1 while level2 was visible; restoration of its contents is
**not yet verified**. `Autosave-111.vsgm1` also exists; its state is not inferred
from its filename. No old control save was copied or loaded in this phase.

After saving, an actual Blue Nullkiller2 turn completed: log records recruitment
of Ash and `Player 1 (blue) ends turn`. Screenshot06 then shows Day2, mana11/12,
movement1500/1500 and unchanged growth; gold20700→21200. This Day2 continuation was
**not separately saved by name** and must not be substituted for the Day1 checkpoint.
No fresh-process restart occurred; it requires the separately requested phase.

Normal client exit0 at519.355s from the original lease start;855 samples contain
no client children/owned INET sockets. Exact client/Xvfb PIDs, display socket/lock
are absent; all1128 payload hashes unchanged. Profile/save hashes retained; original
45aae/f7d control saves and map6eb61681 still match their prior digests. Automatic
Xvfb teardown raced the final input helper's post-click wait, producing XIO; actual
client exit0 is separately verified. No crash or timeout claim from that helper error.
Lease formally released to Build only after these checks. Evidence `testing/linux-c550-gui/`
(summary/profile hashes, retained setup failure, screenshots01–06, actual logs).
Full redesign and restart/Windows gates remain open.

## Final Linux ba85 payload static gate — exact-binary restart still pending

Independent frozen `package-c550-linux-ba85/New-Horizons-Linux-x64` PASS:
1128 readonly files/1127 exhaustive hashes; manifest3e4e7607… and identity7b2ab8be…
match the offer. All912 original candidate files other than identity/sums remain
byte-exact, including both c550 ELFs and all resources. Every added file is
accounted for: exact reviewed notices/terms, committed packaging texts, sanitized
ELF/dependency inventory and actual build provenance. All three source archive
identities match and the binary privacy guard passes.176 system ELFs remain
external; optional driver/dlopen compatibility is not established.

Player TAR19496364 bytes, SHA`2d56479fea0dfd85ef92f3e829ac1e8d198ba8f40f23fd6e2a1239fe9903e8b0`,
contains exactly the1128 payload files, normalized root ownership and retained
read-only444/555 modes/executable bits. Tooling source16796514 bytes,
SHA`b138283369724854a56197beb257f6fa2c0eefbe782ef55cb50a509fceb7e48d`,
matches all4584 exact ba85 Git blobs/types/modes,116 executables, four pinned
submodules and committed export exclusions; mutable future capability code is absent.
Staging source reviewed; six independent offline regressions pass. Packaged
wrapper default routing independently passes explicit `--verify-only`, leaving
the fresh profile absent and never executing a game ELF.

Evidence: `linux-c550-audit/{final-payload-independent,tooling-source-independent}.json`,
rerunnable scripts and `staging-tests-independent.log`/`final-wrapper-independent.log`.
This is bounded static/package approval, **not** publication, exact-binary GUI,
native Windows gameplay or full redesign completion. Await separate GUI grant.

## Earlier Linux c550 dependency/source/text checkpoint

Independent bounded source/packaging-text PASS, not final distribution or GUI
acceptance. All11 acquired source packages /35 original files match exact installed
source versions and both DSC checksums and independently queried local APT-index
SHA256/size records. All22 tar inventories were checked without extraction or
compilation. Three links resolve internally; two contained dangling Android links
are preserved in the exact SDL2 DFSG original archive, not repaired or treated as
Linux runtime dependencies. Initial strict dangling-link assertion is retained.
OpenPGP signature verification is **not** claimed.

Boost3648 headers match original modular preferred sources directly; the remaining
`function_base.hpp` matches after independently replaying declared Debian `60.patch`
(static_cast to reinterpret_cast). All64 oneTBB headers match original sources.
The unpatched one-header difference is retained, not hidden as an original-byte
match. All4471 actual compiler/PCH headers are independently hashed; all19 binary
providers /13 source packages /41 source-file URL/hash pins match the local index.
The eight other API providers now have actual full source/patch archives, not only
URLs. This is not a claim that every generated API header was independently rebuilt.

Dependency source bundle v1:132126986 bytes,
SHA`8459242fc724136b68a33009c993146902429841d0acaacd5ed30879ac3a286f`;
35 exact original files plus3 provenance reports, normalized root ownership and no
personal prefixes in generated JSON. Supplemented v2 independently passes at
132208111 bytes, SHA`25fb2447f6552f98cdf1cecc5a5312314c288aff349ef50f7e2f012f6ecefb00`:
all38 original members unchanged plus all50 exact previously verified supplement
files,88 regular files total; contained paths, normalized ownership and stable
file identity during audit. Evidence `bundle-v2-independent.json`.

Independent per-file review found a concrete overly broad GCC/RLE notice scope:
only386 of394 GCC headers carry RLE text. The other eight are five Apache2 WITH
LLVM-exception PSTL headers, LGPL2-or-later `quadmath.h` with three small inline
bodies, and two unmarked wrappers. Corrected text separates these from oneTBB's
Apache2 **without** LLVM exception. The50-file supplement independently retains
all8 actual GCC headers,35 actual UAPI headers,6 complete specific terms and
provenance. UAPI26 explicit syscall-note identifiers and9 generated/forwarding
headers remain distinguished; syscall-note SHA`8e378ab93586eb55135d3bc119cce787f7324f48394777d00c34fa3d0be3303f`.

All36 sanitized CMake fields match actual cache values; all686 commands use
`/usr/bin/g++` and `-std=gnu++20`, with no explicit plugin/LTO options. Conditional
GCC Eligible Compilation analysis is supported, not a generic system-code waiver.
The Tester's initial strict `c++20` versus actual GNU dialect assumption is retained.
`tools/linux/LEGAL-New-Horizons.txt`33e3601e… and
`tools/linux/BUILDING-New-Horizons.txt`9cede178… have bounded source/text approval:
GPL3 combination, purchaser-art exclusion, nonbundled shared/optional boundary,
actual sources, ordinary rebuilding/relinking and no byte-identical/Git-stamp claim.
No compiler was invoked by Tester. Final documentation/tooling commit, supplemented
source archive, exact staged payload/notices/identity and authorized exact-binary
GUI remain separate gates. **No GUI lease has been granted.**

Evidence: `testing/linux-c550-audit/{source-archives-independent,header-pins-independent,
bundle-build-independent,supplement-independent}.json` and corresponding scripts,
including preserved unpatched/dangling-link/dialect diagnostics. Full redesign,
future capability rendering and native Windows gameplay remain unaccepted.

## Earlier Linux c550 source/static candidate checkpoint — not distribution approval

Independent source companion PASS: `New-Horizons-c5503cc3515a-source.tar.gz`,
16787483 bytes, SHA`f6b625ba2dadbac2473ef18d2f46e8021eb4c6d425ac534be814f4814e1db423`.
All4575 members match exact c550 Git blobs/types/modes, including115 executable
files; four pinned submodule object trees and16 committed export exclusions match.
Design/license are retained, future leadership source absent. Initial Tester cache
location assumption failed and is retained; the corrected reader uses three bare
caches plus pinned dependency Git objects, never mutable working-file bytes.
Evidence `commands-static/c550-source-independent.json` and rerunnable checker.

Readonly `linux-release-c550-origin-candidate` independently has914 files/913
exhaustive checksums,907 resources byte-exact to c550 and matching GPL/attribution/
developer helper. Client`ebb20763…` and facade`3f556fd7…` are AMD64 ELF64; facade
embeds fullc550, both RUNPATHs are exactly `$ORIGIN`, and privacy scan finds zero
profile paths. Static current-host closure resolves178 ELF nodes (176 system) and
1872 client/463 facade strong dynamic imports with versions. This is not client
execution, optional dlopen/driver coverage, generic-Linux compatibility or GUI proof.
Original missing-install-RPATH candidate/RED remain distinct and unchanged.

Read-only Ninja dependency-log analysis inventories4471 compiler/PCH system header
paths,196 separate linker-dependency paths, and182 installed packages with version,
source-package/version and copyright-file hashes; no unowned paths or headers
outside frozen/system roots remain. Linker logs can contain rejected i386 search
candidates: those are **not** required runtime libraries. Recorded compiler inputs
include libgcc.a/libc_nonshared.a/libdl.a and five startup objects; explicit project
archives are separate. All182 copyright files are present locally; that is an
inventory, not review/redistribution approval for every license or source closure.
System-library exceptions, static/header material, applicable notices, optional
plugins and any later bundled libraries still need explicit delivery treatment.
The source-only c550 archive does not settle those third-party obligations.

Build's retained notice tree independently matches all182 package records and
copyright bytes plus17 full common-license texts (200 total files including
provenance). `notices-wrapper-independent.json` records that integrity gate.
Component-level remaining treatment, **not a blanket system-library waiver**:

- glibc2.43-2ubuntu2.3: provide exact source/Ubuntu patches for nonshared/startup
  portions and usable application source/build/relink material. Source acquisition
  is queued. libdl.a is an empty compatibility archive; inspected linker inputs
  alone do not prove every static archive member entered either ELF.
- Boost3649 and oneTBB64 compiled/PCH header paths: retain exact preferred source
  and any generation context, not merely copyright-file presence. Versions are
  boost1.90 1.90.0-6ubuntu1 and onetbb2022.3.0-2. Full header-provider inventory
  spans19 binary packages/13 source packages, including the other API headers.
- GCC15 runtime/startup/C++ headers: assess the specific Runtime Library Exception
  3.1 grant and Eligible Compilation conditions against the recorded stock GCC
  invocation. This is not a presumption that arbitrary compiler plugins or all
  system code are exempt. Relevant complete terms are retained.
- Linux UAPI headers: record their specific Linux-syscall-note permission and retain
  its full text where relied upon; the installed copyright file alone omits it.
- The combined release must accommodate GPLv3: xBRZ's GPL3 source is actually in
  compile_commands, and oneTBB headers are Apache2 without an LLVM exception.
  VCMI's GPL2-or-later grant is not a claim that the combination is GPL2-only.
- The176 external shared ELFs are not bundled. Keep their exact platform dependency
  boundary explicit; shipping any of them later reopens source/license review.

The frozen developer helper's default packaged invocation fails before execution
because it retains the repository build path. Explicit `--client` verify-only
passes and creates no profile. Build's separate `Play-New-Horizons.sh` wrapper has
the correct beside-file default; independent complete launcher/stub regression
passes, without executing a game ELF. Corrected README explicitly limits CC0 to
New Horizons-authored geometry/icons and does not relicense purchaser artwork.
Wrapper77bfaf84…/README06d7cf80… have bounded source approval for separate integration,
not a sandbox claim or final staging approval. The original default RED remains retained.
Final wrapper/README/source staging and an exact-binary guarded GUI run remain
separate gates; **no GUI lease exists** at this checkpoint.

Evidence and rerunnable scripts: `testing/linux-c550-audit/`, especially
`elf-resource-independent.json`, `dependency-inventory-independent.json` and the
launcher logs. This Ubuntu26.04-dependent private candidate is not yet a published
or independently accepted complete Linux distribution.

## Historical baseline inspection

Status: read-only source inspection at `a9b6d945e`; **not runtime acceptance**.
Contract: [NEW_HORIZONS_MVP.md](NEW_HORIZONS_MVP.md). No game/GUI launch,
installation, build, or proprietary-asset inspection was performed for this note.

## Required content and dependencies

| Layer | Required for the curated edition |
| --- | --- |
| Purchaser content | Original **Heroes III Complete** `Data`, `Maps`, and `Mp3` directories, outside Git. Upstream also supports SoD, but that is not this fork's Complete acceptance baseline. HD Edition and native LOKI Linux data are not substitutes. No original executable is needed to play through VCMI. |
| Graphics/text | Original bitmap and sprite LODs, including Complete's AB content. Preserve the installation's localized patch archives rather than assembling a minimal hand-picked collection. |
| Audio/video | Original sound archives, MP3 soundtrack, and video archives for the full presentation/campaign journey. Missing music/video may not block scenario startup; startup alone is not full-content acceptance. |
| Scenarios/campaigns | Purchaser's original `.h3m` maps and original RoE/AB/SoD campaign resources (`.h3c`, including archive-contained resources). No downloaded custom map or fan campaign is needed. |
| Engine resources | Ship the matching upstream `config/`, `scripts/`, and **`Mods/vcmi`** resources with their licenses/notices. `vcmi` is “VCMI essential files,” not the optional `vcmi-extras` download. Virtual `core` contains base configuration/content; retain the internal mod/dependency loader. |
| Runtime/build | Matching client and `vcmi` facade with simulation library and enabled AIs; SDL2 or SDL3 plus image/mixer/ttf, Boost, compression libraries, TBB, and LuaJIT/Lua. Lua and shipped combat scripts are required by this checkout's full-game build (including `scripts/damage/damageCalculator.lua`). FFmpeg supports video. Qt is for launcher/editor, not an original-content asset requirement. Use the build owner's exact resolved dependency list; do not install packages for this inventory. |

Archive discovery is defined by `config/filesystem.json`: `H3ab_bmp.lod` then
`H3bitmap.lod` and applicable localization patches supply `DATA/`;
`H3ab_spr.lod` then `H3sprite.lod` supply `SPRITES/`. Sounds include
`H3ab_ahd.snd`, `Heroes3.snd`, and `Heroes3-cd2.snd`; video candidates include
`H3ab_ahd.vid`, `Heroes3.vid`, and `video.vid`. These are supported installation
variants, **not a requirement that every named archive exist in every edition**.
The configured `H3psprit.lod` entry is deliberately disabled upstream. Keep that
behavior. Loose Data/Sprites/Sounds/Video files can override archive resources.

No original assets, executables, installers, extracted files, saves, or map copies
are to be committed or redistributed. Preserve VCMI GPL notices, attribution and
resource-specific licensing; GPL coverage is not permission to distribute Heroes
III content. Naming/distribution rights remain separately reviewable.

## Discovery and contamination checks

- Native Linux: `lib/VCMIDirs.cpp` selects installed data roots (`M_DATA_DIR`,
  reversed `$XDG_DATA_DIRS` plus `/vcmi`, or standard share paths, also
  `/usr/share/games/vcmi`). Development mode uses `.` instead of system roots.
  `lib/filesystem/Filesystem.cpp::createInitial` then adds user data, normally
  `$XDG_DATA_HOME/vcmi` or `~/.local/share/vcmi`. Saves are under its `Saves/`;
  settings normally use `$XDG_CONFIG_HOME/vcmi` or `~/.config/vcmi`.
- Resource names are case-insensitive; later sources win. Record **all effective
  roots**, not just the intended asset directory. Existing user Mods, loose
  overrides and presets can silently contaminate a supposedly clean baseline.
  Arrange an approved isolated runtime profile later without deleting or modifying
  the purchaser's data or other VCMI installations; this note creates none.
- Launcher `FirstLaunchView::heroesDataDetect` checks `DATA/GENRLTXT.TXT` and
  `DATA/TENTCOLR.TXT` (with a demo exception). This is only a rough presence check.
  `StartGameTab::refreshGameData` probes town backgrounds, AB campaigns,
  `Music/MainMenu`, and `Video/H3Intro`. Its missing-files/campaign flags currently
  start false and use `&=`: absence of those warnings does **not** prove completeness.
- Windows additionally has launcher registry-based installation suggestions;
  actual resource roots remain controlled by `VCMIDirs`/filesystem configuration.
  Do not infer that a neighboring original installation is automatically mounted.

## Optional content and default traps

- Baseline active root mod: `vcmi` (`lib/modding/ModManager.cpp` creates this fresh
  preset). Exclude `roe-demo`, `vcmi-test`, third-party expansions, translation
  downloads, HD packs, Chronicles and custom maps from first-gate evidence.
  Retain dependency/version validation rather than bypassing it.
- `Mods/vcmi/mod.json` includes engine fixes, translations and RMG templates,
  including HD-mod/symmetric template families. Do not delete the essential mod
  wholesale in pursuit of “no Mods.” Use an original fixed scenario first;
  any later template-selection curation needs separate review.
- Launcher first-run presets **preselect `vcmi-extras`** and offer HotA, WoG and
  other fan content (`launcher/firstLaunch/firstlaunch_moc.cpp`). These are optional,
  not required original-content dependencies. Translation suggestions can also
  introduce downloads. New Horizons must remove that optional installation journey;
  this document proposes policy, not a completed UI/config change.
- `config/schemas/settings.json` enables repository checks/updates by default.
  Those are launcher services, not prerequisites for single-player. `lastMap`
  defaults to `Maps/Arrogance`: select an actually installed original scenario
  explicitly, especially with localized filenames. `useHdTextures=true` does not
  require a texture pack: renderers check availability and fall back. Intro/video
  defaults do require working original media plus video support for full fidelity.
- Gameplay AI defaults are **Nullkiller2/BattleAI**, not MMAI. Nevertheless root
  CMake defaults `ENABLE_MMAI=ON`, introducing an ONNX Runtime build dependency
  (`AI/MMAI/CMakeLists.txt`). It can be disabled by the build owner for the MVP;
  no ML-content acquisition belongs in the ordinary acceptance route.

## Ordinary full-game route — pending bounded run authorization

Record platform, source revision/dirty state, assigned build/candidate identity,
renderer, effective data/settings/save roots, original edition/language, active
content and selected original map/settings in local evidence. Do not put private
absolute paths or proprietary artifacts in Git. Use normal menu/mouse/keyboard
input, no cheats, direct state mutation, test-only maps or new harness framework.

1. Open the ordinary menu → New Game → Single Player → scenario selection.
   Choose an original scenario with one human, at least one AI, an accessible town,
   resources and reachable combat. Record hero/faction/difficulty; obtain a
   spellbook, learned combat spell and sufficient mana through ordinary play.
2. Start; select a hero, plot and complete multi-step movement, pick up resources
   and verify inventory changes. Enter town, construct a legal building, recruit
   troops and verify costs, army and dwellings. Check graphics, text and audio.
3. Play **at least seven turns**, including AI completion and the week boundary;
   verify income/growth and regained movement. Fight manually and cast a spell;
   check animation, effects, casualties and return to the adventure map.
4. Use the normal Save Game dialog and wait for success. Record day, hero
   position/stats/mana/army/artifacts, resources, town buildings and ownership.
   Quit the application, restart the same candidate/profile, then Load Game →
   Single Player and select that save. Compare recorded state and continue moving,
   visiting town, ending turns and fighting. Also check a normal autosave reload.
5. Continue to a genuine victory or defeat and the result/return-to-menu flow.
   Observe no separate `vcmiserver` child or single-player TCP/UDP listener or
   loopback transport during selection, start, save/load, play and shutdown.
   Record errors, stalls and missing media explicitly; source inspection alone
   cannot establish this condition or graphical usability.

Save/load anchors: `config/mainmenu.json` routes `start single`/`load single`;
`client/lobby/SelectionTab.cpp` enumerates `Saves/`; `CMapInfo::saveInit` reads
headers/options. `CCallback::save` sends `SaveGame` through server validation;
`CGameHandler::save/load` writes/reads `.vsgm1`, game state and server state.
`ActiveModsInSaveList` verifies gameplay-affecting content and rejects missing,
disabled or excessive root mods. Keep the same candidate/content for the baseline:
no promise of importing original Heroes III saves or arbitrary upstream versions.

Campaign continuity is a follow-up original-content regression: New Game → Campaign
→ RoE/AB/SoD, choose scenario/bonus, save/quit, Load Game → Campaign, finish a
scenario and verify unlocks and permitted hero carryover into the next scenario.
Custom campaigns/Chronicles are not prerequisites. The first scenario gate alone
does not prove all original campaigns completable.

## References read

Upstream [Code Structure](developers/Code_Structure.md),
[Serialization](developers/Serialization.md), [Networking](developers/Networking.md),
[Linux build](developers/Building_Linux.md) and
[Linux installation](players/Installation_Linux.md), checked against current source.
Some upstream prose is historical (notably TCP even for single-player and serializer
version details); the New Horizons contract and current implementation take precedence.
